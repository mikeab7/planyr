{
  "tolerance": {
    "maxDiffPct": 0.02,
    "maxChannelDelta": 8
  },
  "surfaces": {
    "map-landing": {
      "light": {
        "approvedAt": "2026-09-05",
        "approvedCommit": "a3c775b2",
        "note": "B1176976: HelpReportControl's floating help/report FAB corrected from RADIUS.pill (999, a full circle) to RADIUS.md (8) per docs/DESIGN.md's shape rule — pill is reserved for containers, this is a standalone control. Only the button's own corner curve changes (44x44 hit area, popover anchoring, position all unchanged)."
      },
      "dark": {
        "approvedAt": "2026-09-05",
        "approvedCommit": "a3c775b2",
        "note": "B1176976: HelpReportControl's floating help/report FAB corrected from RADIUS.pill (999, a full circle) to RADIUS.md (8) per docs/DESIGN.md's shape rule — pill is reserved for containers, this is a standalone control. Only the button's own corner curve changes (44x44 hit area, popover anchoring, position all unchanged)."
      },
      "light--phone": {
        "approvedAt": "2026-09-05",
        "approvedCommit": "a3c775b2",
        "note": "B1176976: HelpReportControl's floating help/report FAB corrected from RADIUS.pill (999, a full circle) to RADIUS.md (8) per docs/DESIGN.md's shape rule — pill is reserved for containers, this is a standalone control. Only the button's own corner curve changes (44x44 hit area, popover anchoring, position all unchanged)."
      },
      "dark--phone": {
        "approvedAt": "2026-09-05",
        "approvedCommit": "a3c775b2",
        "note": "B1176976: HelpReportControl's floating help/report FAB corrected from RADIUS.pill (999, a full circle) to RADIUS.md (8) per docs/DESIGN.md's shape rule — pill is reserved for containers, this is a standalone control. Only the button's own corner curve changes (44x44 hit area, popover anchoring, position all unchanged)."
      }
    },
    "site-planner-header": {
      "light": {
        "approvedAt": "2026-09-05",
        "approvedCommit": "a3c775b2",
        "note": "B1176976: HelpReportControl's floating help/report FAB corrected from RADIUS.pill (999, a full circle) to RADIUS.md (8) per docs/DESIGN.md's shape rule — pill is reserved for containers, this is a standalone control. Only the button's own corner curve changes (44x44 hit area, popover anchoring, position all unchanged)."
      },
      "dark": {
        "approvedAt": "2026-09-05",
        "approvedCommit": "a3c775b2",
        "note": "B1176976: HelpReportControl's floating help/report FAB corrected from RADIUS.pill (999, a full circle) to RADIUS.md (8) per docs/DESIGN.md's shape rule — pill is reserved for containers, this is a standalone control. Only the button's own corner curve changes (44x44 hit area, popover anchoring, position all unchanged)."
      },
      "light--phone": {
        "approvedAt": "2026-09-05",
        "approvedCommit": "a3c775b2",
        "note": "B1176976: HelpReportControl's floating help/report FAB corrected from RADIUS.pill (999, a full circle) to RADIUS.md (8) per docs/DESIGN.md's shape rule — pill is reserved for containers, this is a standalone control. Only the button's own corner curve changes (44x44 hit area, popover anchoring, position all unchanged)."
      },
      "dark--phone": {
        "approvedAt": "2026-09-05",
        "approvedCommit": "a3c775b2",
        "note": "B1176976: HelpReportControl's floating help/report FAB corrected from RADIUS.pill (999, a full circle) to RADIUS.md (8) per docs/DESIGN.md's shape rule — pill is reserved for containers, this is a standalone control. Only the button's own corner curve changes (44x44 hit area, popover anchoring, position all unchanged)."
      }
    },
    "site-planner-left-rail": {
      "light": {
        "approvedAt": "2026-09-05",
        "approvedCommit": "a3c775b2",
        "note": "B1176976: HelpReportControl's floating help/report FAB corrected from RADIUS.pill (999, a full circle) to RADIUS.md (8) per docs/DESIGN.md's shape rule — pill is reserved for containers, this is a standalone control. Only the button's own corner curve changes (44x44 hit area, popover anchoring, position all unchanged)."
      },
      "dark": {
        "approvedAt": "2026-09-05",
        "approvedCommit": "a3c775b2",
        "note": "B1176976: HelpReportControl's floating help/report FAB corrected from RADIUS.pill (999, a full circle) to RADIUS.md (8) per docs/DESIGN.md's shape rule — pill is reserved for containers, this is a standalone control. Only the button's own corner curve changes (44x44 hit area, popover anchoring, position all unchanged)."
      },
      "light--phone": {
        "approvedAt": "2026-09-05",
        "approvedCommit": "a3c775b2",
        "note": "B1176976: HelpReportControl's floating help/report FAB corrected from RADIUS.pill (999, a full circle) to RADIUS.md (8) per docs/DESIGN.md's shape rule — pill is reserved for containers, this is a standalone control. Only the button's own corner curve changes (44x44 hit area, popover anchoring, position all unchanged)."
      },
      "dark--phone": {
        "approvedAt": "2026-09-05",
        "approvedCommit": "a3c775b2",
        "note": "B1176976: HelpReportControl's floating help/report FAB corrected from RADIUS.pill (999, a full circle) to RADIUS.md (8) per docs/DESIGN.md's shape rule — pill is reserved for containers, this is a standalone control. Only the button's own corner curve changes (44x44 hit area, popover anchoring, position all unchanged)."
      }
    },
    "library": {
      "light": {
        "approvedAt": "2026-09-05",
        "approvedCommit": "a3c775b2",
        "note": "B1176976: HelpReportControl's floating help/report FAB corrected from RADIUS.pill (999, a full circle) to RADIUS.md (8) per docs/DESIGN.md's shape rule — pill is reserved for containers, this is a standalone control. Only the button's own corner curve changes (44x44 hit area, popover anchoring, position all unchanged)."
      },
      "dark": {
        "approvedAt": "2026-09-05",
        "approvedCommit": "a3c775b2",
        "note": "B1176976: HelpReportControl's floating help/report FAB corrected from RADIUS.pill (999, a full circle) to RADIUS.md (8) per docs/DESIGN.md's shape rule — pill is reserved for containers, this is a standalone control. Only the button's own corner curve changes (44x44 hit area, popover anchoring, position all unchanged)."
      },
      "light--phone": {
        "approvedAt": "2026-09-05",
        "approvedCommit": "a3c775b2",
        "note": "B1176976: HelpReportControl's floating help/report FAB corrected from RADIUS.pill (999, a full circle) to RADIUS.md (8) per docs/DESIGN.md's shape rule — pill is reserved for containers, this is a standalone control. Only the button's own corner curve changes (44x44 hit area, popover anchoring, position all unchanged)."
      },
      "dark--phone": {
        "approvedAt": "2026-09-05",
        "approvedCommit": "a3c775b2",
        "note": "B1176976: HelpReportControl's floating help/report FAB corrected from RADIUS.pill (999, a full circle) to RADIUS.md (8) per docs/DESIGN.md's shape rule — pill is reserved for containers, this is a standalone control. Only the button's own corner curve changes (44x44 hit area, popover anchoring, position all unchanged)."
      }
    }
  }
}
