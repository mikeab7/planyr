## ✅ Done

### B196 — Configurable table columns (show / hide / add / reorder), MS Project / P6 style `[Scheduler / Table]` (feature)  *(arrived as "NEW-3"; minted B196 — next free ID after B195)*
`[x]` **Done 2026-06-19 (branch `claude/sharp-keller-wphsna`).** The Schedule's left table panel (`GridView` in `public/sequence/index.html`) columns are now user-customizable. **Show / hide / add / reorder / resize** — reorder (header drag) and resize (header-edge drag + double-click auto-size) already existed; this adds show/hide + an available-field set. **Two entry points, both portal-mounted at document root** (the established z-index pattern): a discoverable **"⊞ Columns" button** pinned to the table's top-right opening a field chooser (`GridColumnsChooser` — *Shown columns* with ▲▼ reorder + ✕ hide, the Task-name column **locked**-visible; *Available — click to add*; *Reset to default*), **plus** a right-click header **context menu** (`GridHeaderMenu` — *Hide this column* / *Insert column ▸* submenu of hidden fields / *Columns…*). **Available fields** add **% Complete** and the **cost/budget** trio (**Budget, Cost, Actual / Spend**); cost columns render **blank** for empty cells and **roll up to parents** (`rolledBudget`/`rolledActual` mirror `rolledCost`) — never an error on a project with no money entered. The new fields **ship hidden by default**; the End column label is aligned to **"Finish"** (MSP/P6 term; the `end` key is unchanged). **Per-project persistence:** column config (visible set, order, widths) lives at `projects[pid].colConfig` and rides the **existing cloud-save path** (`writeGridCfg` → `setData` → autosave); each project is **independent**, new/other projects start from `DEFAULT_GRID_COLS`, and legacy global `gridColOrder`/`gridColWidths` migrate in on first read (`readGridCfg`). Keyboard column nav (arrow/Tab, in-grid and in-cell-edit) **clamps to the visible columns** so the cursor can't wander into hidden fields; SplitView's divider column-snap follows the visible order. COLS stays the master registry + selection index space, so cell selection / range / copy are unchanged.
> **Self-verified headless** (logged-out seed; `ui-audit/verify-sequence.mjs` + interaction probes): chooser opens and adding **Budget**/**Actual** makes them appear (header count 9→11); the header context menu opens with the **Insert** submenu listing hidden fields; and **per-project independence holds** — Budget added to *Goose Creek* is **absent** on *Bee Sand Development* and **persists** on return. 0 runtime console errors; the B194/B195 bars are unaffected. **Logged-in cloud round-trip** rides the identical `setData`→`cloudSync` path as task edits — the sandbox proxy blocks sign-in, so a signed-in pass is the one remaining confirmation (logged-out, the existing "changes will not save" banner keeps persistence honest, no silent revert). lint **0 errors** · **352 tests** · build green. **(Optional/future, flagged not-built per the brief:** auto-default cost columns visible for Pursuit-lifecycle projects — left manual.)
> **Dedup:** mirrors **MasterView's** existing `visCols` show/hide pattern (`masterCols`), generalized to the per-project table with reorder + an explicit chooser/menu. Distinct from **B159** (export task-name toggle) and **B163** (a `progress_pct` data-model field). No new store — reuses the project record + the cloud-save path.

### B195 — Task status as fill treatment (hollow / partial / solid) `[Scheduler / Gantt]` (feature)  *(arrived as "NEW-2"; minted B195 — next free ID after B194)*
`[x]` **Done 2026-06-19 (branch `claude/sharp-keller-wphsna`).** Task status moved **off the color channel onto a single continuous fill treatment** in both Gantt render paths (on-screen `GanttView` + the PDF/print `buildGanttSVG`). Every task bar keeps **one identity hue** (the existing task **gray `#94a3b8`**, or an explicit per-task `rowColor` override); status reads as **0→100% complete**: **not started (0%)** = hollow rounded pill (outline in the hue, required else invisible); **in progress** = partial fill (solid portion = `percentComplete`, lighter remainder behind it, thin outline around the whole); **complete (100%)** = solid fill. A **min-width floor** keeps a 0% bar from collapsing to a sliver. Fill fraction derives from `percentComplete` (Complete → solid regardless). The **red / paused / overdue** signal stays in the **row background + the table** (untouched), so status never collides with the navy hierarchy. Milestone diamonds follow the same monochrome language (gray, filled when complete, hollow otherwise).
> **Self-verified headless** (`ui-audit/verify-sequence.mjs`): DOM probe finds **11 solid + 24 outlined** gray task bars and **0** bars using the old health colors (yellow/red/green); 0 runtime errors. Palette preserved (the one purple `SS` dependency line `#7c3aed` untouched). lint 0 · 352 tests · build green.

### B194 — Summary bars as depth-stepped brackets `[Scheduler / Gantt]` (feature)  *(arrived as "NEW-1"; minted B194 — highest B# across both files was B193, so this is the real next free ID)*
`[x]` **Done 2026-06-19 (branch `claude/sharp-keller-wphsna`).** Replaced the heavy solid summary bars (a thick slab + barely-visible triangle endcaps) with a thin **bracket**: a navy horizontal **span** with **straight-down vertical legs** at each end (no triangles), in both render paths (`GanttView` + `buildGanttSVG`). WBS nesting depth is encoded by **three cues that step together, top-of-tree strongest** — **darkness** (navy `#2B3340` → `#46506A` → `#6E7790` → tint), **span thickness** (7/5/4/4 px), and **leg length** (a fraction of row height) — the ramp **stops deepening at ~4 levels** (the table's indented name carries depth from there). The summary **name centers just above the span** in the bar's own level-navy (no thick fill to print white onto); no per-level label indentation (bars are date-positioned). An explicit `rowColor` override still wins.
> **Self-verified headless** (`ui-audit/verify-sequence.mjs`): DOM probe finds **36 navy bracket elements** (≈ a dozen brackets × span+2 legs); a detail screenshot confirms the shape — label above a thin span with a straight-down leg, **no triangle**. 0 runtime errors. lint 0 · 352 tests · build green.

### B193 — Persist current project before switching `[Header/Nav]` (task)  *(arrived as "NEW-3"; filed provisionally as B191, renumbered **B193** — concurrent `main` (#168) took B189–B190 for the Site Analysis tranche, so this is the real next free ID after B190/B191/B192)*
`[x]` **Done 2026-06-19 (branch `claude/awesome-gauss-jlrq5v`).** A breadcrumb switch (to another project or to the Dashboard) flushes the current project first, so a switch can't silently drop unsaved work — it routes through the **existing** data-loss machinery: in the Site Planner, opening another project goes through `handleOpenSite` → `flushSite()` and the `key`-change unmount `persistOrDrop()`; in Doc Review, the `useReviewPersistence` unmount/visibility flush already guarantees the local write. On top of that, B193 adds the owner-requested **cloud-at-risk surfacing** (the B124/B125 silent-failure rule): each workspace passes a normalized `saveState` to the breadcrumb, and when it's `offline`/`error`, the switcher shows a **passive ⚠ line in the dropdown** AND a **transient "saved on this device — will sync when reconnected" toast** on the switch. The switch is **never blocked** on a successful cloud save, but is **never silent** about an at-risk one.
> **Self-verified headless** (`ui-audit/verify-new1to3.mjs`): project switches complete cleanly; the at-risk surfacing is gated on `offline`/`error` (the sandbox is logged-out → `local`, no false warning). **Dedup:** no new persistence path — reuses SitePlanner's `flushSite`/`persistOrDrop` + Doc Review's persistence flush; B193 is the routing + surfacing layer over them. 344 tests · lint 0 · build green.

### B192 — "Dashboard" breadcrumb root → all-projects view `[Header/Nav]` (feature)  *(arrived as "NEW-2"; filed provisionally as B190, renumbered **B192** — concurrent `main` (#168) took B189–B190 for the Site Analysis tranche, so this is the real next free ID after B190/B191)*
`[x]` **Done 2026-06-19 (branch `claude/awesome-gauss-jlrq5v`).** The left crumb is a **literal-text "Dashboard"** label with a `ti-layout-dashboard` icon, always visible alongside the project crumb so the **Dashboard > Project** hierarchy is self-evident. It routes to the all-projects landing — the **Site Planner map** (the canonical all-projects surface: one marker per project + the YOUR SITES list). From Markup/Schedule it switches into the Site module's map via the shell's `navIntent`; in the Site Planner it sets map mode directly. The **logo + wordmark also route to the Dashboard** (secondary path; its tooltip now reads "Dashboard — all projects"), but the labeled crumb is primary and the logo is no longer the only way back. The **"All projects (Dashboard)"** row pinned atop the B191 dropdown is the third, backstop path.
> **Self-verified headless** (`ui-audit/verify-new1to3.mjs`): from a loaded project, clicking the Dashboard crumb returns to the all-projects map; the crumb is present in Markup + Schedule. **Dedup:** the logo already routed to the map; B192 makes that route labeled + discoverable + shared across all three workspaces. 344 tests · lint 0 · build green.

### B191 — Project switcher breadcrumb in header Row 1 `[Header/Nav]` (feature)  *(arrived as "NEW-1"; filed provisionally as B189, renumbered **B191** — concurrent `main` (#168) took B189–B190 for the Site Analysis tranche, so this is the real next free ID after B190)*
`[x]` **Done 2026-06-19 (branch `claude/awesome-gauss-jlrq5v`).** Added a left-anchored `Dashboard / <Project> ▾` breadcrumb to the shared two-row `AppHeader`, immediately right of the logo+wordmark, used identically by Site / Schedule / Markup. New shared component `src/shared/ui/ProjectBreadcrumb.jsx`: the project crumb opens a **portal-mounted `AnchoredMenu`** dropdown containing — top to bottom — a **search field** (filters by name), an **"All projects (Dashboard)"** row pinned at top + a divider, the **recent-projects list newest-edited-first with relative timestamps** ("just now / 5m ago / 3h ago / 2d ago / 3w ago / short date"), and a **"New project"** action at the bottom. The list comes from `listProjects()` (`src/shared/projects/projects.js`) which groups the EXISTING per-user, RLS-scoped `loadSitesList()` store via the pure `groupProjects` helper (`src/shared/projects/projectModel.js`) — **no parallel project store**, only the signed-in user's own projects, group-id-backed (name is a label only). Selecting a project loads it **in place** and stays in the current workspace where applicable (Site → opens the plan; Markup → browses that project's files via the Files drawer; Schedule → routes into the Site Planner). **Pure helpers covered by 10 new unit tests** (`test/projects.test.js`).
> **Self-verified headless** (`ui-audit/verify-new1to3.mjs`, logged-out, 3 seeded projects): breadcrumb renders in all three workspaces; dropdown is portal-mounted **outside `<header>`**, lists projects **newest-first** with timestamps, search filters by name, and selecting a project loads it. **Dedup:** distinct from the shipped B10/B104 (header shell + module tabs). The Site Planner plan-mode **center** site▾›plan▾ switcher (rename + version history) stays as the in-site plan-level detail; the left breadcrumb is the app-wide project switcher. **Known cosmetic follow-up:** in the planner the project name now shows in both the left breadcrumb and the center switcher — flagged to the owner (streamlining the center switcher trades off its in-place site-rename affordance, so it's a deliberate owner call, not auto-applied). 344 tests · lint 0 · build green.

### B190 — Click a constraint card to toggle its layer on the map `[Site Planner / Site Analysis]` (feature)  *(arrived as "NEW-2"; filed provisionally as B185, renumbered **B190** — concurrent `main` (#164) took B184–B188 for unrelated Site Planner items, so this is the real next free ID after B189)*
`[x]` **Done 2026-06-19 (branch `claude/bold-edison-h8tfkc`).** Each resolved constraint card now carries a **◍ Map** toggle; clicking it shows that category's GIS layer on the planner map, framed to the site; **◉ On map** while shown; click again to hide.
> **Built on the existing shared overlay system — NO parallel renderer.** Each source in `lib/siteAnalysis.js` gained a `mapLayer` id pointing at the layer that already exists in `lib/layers.js` (`flood→fema`, `wetlands→wetlands`, `pipelines→txrrc_pipe`, `oilgas→txrrc_wells`); the finding carries it through. `SiteAnalysis.jsx` renders the toggle **only** for a category that **resolved (present/absent) AND has a `mapLayer`** — so UNKNOWN / info / no-source categories (Road authority, Contamination, Jurisdiction, Zoning) show **no blank toggle**, their error stays surfaced (the bug report's rule). The toggle writes the same app-shared **`overlays`/`setOverlays`** state the Layers panel uses (one source of truth → `syncOverlayLayers` paints it), and `SitePlanner.toggleAnalysisLayer` turns the basemap on + **`frameToActiveParcels()`** (active-parcel extent + 60% margin, since FEMA/NWI are scale-gated). Independent per-card toggles → multiple layers at once. If a just-enabled layer's map service fails, the card shows an honest "service isn't responding" hint (the screen result still stands).
> **Verified live (headless):** clicking Wetlands **◍ Map** flipped to **◉ On map** and added the NWI overlay (`leaflet-image-layer` 0→1), framed to the parcel — screenshot `ui-audit/screens/analysis-verify.png`. lint 0 errors · 341 tests · build green · doc-review/site-planner lazy-chunk split intact. **V45.**
> **Dedup:** distinct from **LayerPanel** (the full layer checklist) — this is a per-finding shortcut into the same `overlays` state; same explicit-click pattern as the jurisdiction identify (never auto-loads all layers). Builds on **B189** (a card only offers the toggle once its query resolves).

### B189 — Site Analysis constraint queries failing (UNKNOWN / "Failed to execute query") `[Site Planner / Site Analysis]` (bug)  *(arrived as "NEW-1"; filed provisionally as B184, renumbered **B189** — concurrent `main` (#164) took B184–B188 for unrelated Site Planner items, so this is the real next free ID after B188)*
`[x]` **Done 2026-06-19 (branch `claude/bold-edison-h8tfkc`).** Filed AND fixed AND verified live the same session. Bug: on Site Analysis, **Wetlands** + **Pipelines** returned "Failed to execute query." → UNKNOWN, while Floodplain worked. The bug report's instinct to **audit every category, not just the two visible**, paid off: a **third** source (**Oil & gas wells**, below the fold) was 400-ing too.
> **Root cause was a different field bug per source — confirmed live (HTTP 400 with old fields → HTTP 200 with real features) against a known Katy/Sheldon-Lake parcel before patching:**
> - **Wetlands** — NWI split layers 1/2 are **JOINED** layers (`Wetlands ⋈ NWI_Wetland_Codes`), so every field comes back **table-qualified** (`Wetlands_CONUS_West.WETLAND_TYPE`); asking for the bare names 400'd, and the qualifier **differs per sublayer** (East vs West). Fix: request `outFields:"*"` and **`normalizeAttrs()`** strips the `Table.` prefix so the summarizers read plain names.
> - **Pipelines** — queried `OPERATOR`/`COMMODITY`, which **don't exist** (real: `OPER_NM`, `CMDTY_DESC`, `DIAMETER`).
> - **Oil & gas wells** — queried `LEASE_NAME`, which **doesn't exist** on the Wells layer (real: `SYMNUM`/`API`/`WELLID`/…).
> All three fixed + flipped **`verified:true`**, so a now-working empty query resolves to **None found** (not conservative UNKNOWN) — matching the bug report's "expected." The silent-error guard is intact: a genuine HTTP/network error still classifies **unknown**, never a fabricated "none." Added **diagnostics** — `defaultFetchJson` carries `{httpStatus,url,arcgisCode}` on the Error and `logQueryFailure()` prints the real failure to the console, so an opaque "Failed to execute query." is debuggable instead of silent.
> **Verified live (headless, `ui-audit/verify-analysis.mjs`, real USFWS/FEMA/TxRRC endpoints):** Floodplain **PRESENT (Zone AE)**, Wetlands **PRESENT (Lake, Freshwater Forested/Shrub Wetland)**, Pipelines **NONE FOUND**, Oil & gas **NONE FOUND** — zero "Failed to execute query." 341 tests (siteAnalysis 25→36, incl. the qualified-field + corrected-field + normalizeAttrs cases), lint 0 errors, build green. **V45.**
> **Dedup:** this is the per-source debug of **B147**'s "🔲 Remaining (1)." Distinct from **B133/B135** (those fixed the wetlands *map-overlay image* `/export`; this fixed the Site Analysis `/query` connector — a different request path on the same host).

### B188 — Trailer-parking label inconsistent + should run in-line on the strip `[Site Planner]` (bug)  *(arrived as "NEW-5"; filed provisionally as B184, renumbered **B188** — concurrent `main` took B180–B183 for unrelated items, so this is the real next free ID.)*
`[x]` Done 2026-06-19 (branch `claude/happy-goldberg-piym6g`). Two trailer-parking instances rendered their labels differently — the wide field kept its 3-line label horizontal, while the narrow wall strip overflowed (the collision engine leadered it OUT of the strip). Root cause: there was no orientation logic, so a label wider than the strip was pulled outside rather than turned to run along it. Fix = the **shared label-orientation helper** built for B186 (`stripLabelRot`, applied to `landscape`/`sidewalk`/`trailer` strips): when a label is wider than the strip is narrow, it runs ALONG the strip's long axis (vertical text on a vertical strip), so both instances now render consistently in-line and neither claims more space than the strip itself. **Self-verified headless** (`ui-audit/verify-new1to5.mjs`): a thin vertical trailer strip shows its label running vertically down the strip; a wide trailer field keeps its label horizontal. 286 tests · lint 0 · build green.

### B187 — Truck court resize must anchor to the dock wall and push/pull trailer parking `[Site Planner]` (bug) — high severity  *(arrived as "NEW-4"; filed provisionally as B183, renumbered **B187**.)*
`[x]` Done 2026-06-19 (branch `claude/happy-goldberg-piym6g`). Changing a truck court's **Depth** field (e.g. 135→140) resized it from the **centre** — losing depth on both sides, detaching the paving from both the dock wall and the trailer parking (a silently-wrong geometry state). **Two root causes fixed:** (1) `resizeSelEl` (the numeric Width/Depth editor) kept `cx/cy` fixed, so it grew/shrank symmetrically — it now applies the same **`hostClampOf`/`clampToHost`** host-edge pin the resize grips already use, so an attached strip/court stays flush against the dock wall and only the FAR edge moves; (2) `refitChildren` never moved the court's back-side trailer when the court itself was the resized element (it only handled trailers when the *building* resized) — it now registers a resized truck court in `courtBox`, so the `forCourt` trailer follows the moved far edge (pushed out on grow, pulled in on shrink, spacing preserved). Covers both the numeric field and the resize grips. **Self-verified headless** (`ui-audit/verify-new4b.mjs`, the owner's exact path — Depth 135→170): the building/dock-wall label moved **0 px** (court stays anchored) while the trailer label moved **+54 px** out (pushed flush) — court → court paving → trailer stay connected, nothing detaches. 286 tests · lint 0 · build green.

### B186 — Buffer-strip width labels run along the strip's long axis `[Site Planner]` (feature)  *(arrived as "NEW-2"; filed provisionally as B182, renumbered **B186**; shares its helper with B188/NEW-5)*
`[x]` Done 2026-06-19 (branch `claude/happy-goldberg-piym6g`). A "5′ Landscape" / "5′ Sidewalk" label rendered horizontally even on a thin vertical strip, eating canvas across the strip. Added one shared, holistic helper — **`stripLabelRot(el, lines, charW, ppf)`** (covers `landscape`, `sidewalk` AND `trailer`, no per-type duplication) — that returns a rotation when the label is wider than the strip is narrow: the label then runs ALONG the strip's long axis (vertical text on a vertical strip, horizontal on a horizontal one). The shared `labelLayout` collision engine was extended to test the label's **rotated** on-screen footprint, so a vertical label is fitted against the strip's tall height and narrow width (and so it lands INSIDE the strip instead of being leadered out). New unit test in `test/labelLayout.test.js`. **Self-verified headless** (`ui-audit/verify-new1to5.mjs`): vertical landscape/sidewalk/trailer strips show vertical labels; horizontal strips and wide fields stay horizontal. 286 tests · lint 0 · build green.

### B185 — Site acreage chip is draggable + persisted `[Site Planner]` (feature)  *(arrived as "NEW-3"; filed provisionally as B181, renumbered **B185**.)*
`[x]` Done 2026-06-19 (branch `claude/happy-goldberg-piym6g`). The parcel acreage chip ("32.51 ac") was pinned at the parcel centroid and obstructed the layout. It's now **click-and-drag** (in Select), to anywhere — including outside the parcel boundary — storing a `labelOffset` (parcel-local feet) on the parcel so it survives geometry edits. New `startAcChip` + an `acChip` drag-move case mirror the B146 dimension-callout drag pattern; the offset persists via the existing Site Model save path (additive field on `parcels[]`, mirrors to Supabase when signed in). **Self-verified headless** (`ui-audit/verify-new3.mjs`): the chip dragged 182 px and `parcels[0].labelOffset` was written to the saved store. 286 tests · lint 0 · build green.

### B184 — 24′ road-width dimension drifts left of centre `[Site Planner]` (bug)  *(arrived as "NEW-1"; filed provisionally as B180, renumbered **B184** — concurrent `main` took B180–B183.)*
`[x]` Done 2026-06-19 (branch `claude/happy-goldberg-piym6g`). The road-width dimension ("24′") rendered shifted toward the left edge of the road instead of centred on the span. Cause: the dimension line/label were placed at **18 %** along the element's long axis (a sensible default for a building's depth tick, but wrong for a road, where the width tick should sit on the centreline of the length). Fix: roads now place the width dimension at the **midpoint (50 %)** of the length (`posF = el.type === "road" ? 0.5 : 0.18`); the measured value and the perpendicular span anchor were already correct (the label sits at the midpoint of the measured width), so this is a position-only fix — no measurement-accuracy change. **Self-verified headless** (`ui-audit/verify-new1to5.mjs`): the "24′" label sits centred along the road. 286 tests · lint 0 · build green.

### B183 — Editable layers decouple from backdrop during pan while loading `[Site Planner]` (bug)  *(arrived as "NEW-1"; filed provisionally as B177, renumbered **B183** — concurrent `main` took B176–B181, so this is the real next free ID after B181/B182.)*
`[x]` **Done 2026-06-19 (branch `claude/nice-heisenberg-r6lmf1`).** On a freshly-loaded site, grabbing and panning before load settled made the drawn layers (massing/parcels/markup) visibly offset from the aerial for a frame or two, then snap back. **Root cause (confirmed, not the pasted race-condition theory):** in Planyr the SVG *is* the source of truth and the basemap follows it, so the layers can't reproject against a stale transform — the decoupler was **B65's own ghost snapshot**. A `commit` clones the basemap into a frozen overlay held until the fresh tiles `load`; while loading, that `load` is delayed so the snapshot lingered, and a pan moved the live SVG while the **frozen** snapshot stayed put → the aerial lagged the drawn layers until the snapshot expired (hence "only early on, then snaps back"). **Fix:** drop any lingering snapshot the instant a new gesture starts (`dropGhost()` at the top of the transform path), so a pan is tracked by the live CSS transform (welded to the SVG) and the backfill covers any reveal; a fresh snapshot is taken only at the next commit. **Verified headless** (`ui-audit/diagnose-decouple.mjs`, retina + throttled, pan immediately after load): **0 %** of pan frames have a snapshot on top (idle baseline has one, correctly dropped on the first pan). lint 0 · 257 tests · build green.

### B182 — Basemap tiles missing at high zoom ("Map data not yet available") `[Site Planner]` (bug)  *(arrived as "NEW-1"; filed provisionally as B176, renumbered **B182** — concurrent `main` took B176–B181 (jurisdictions/tax + cost takeoff), so this is the real next free ID.)*
`[x]` **Done 2026-06-19 (branch `claude/nice-heisenberg-r6lmf1`).** Zooming in deep showed gray "Map data not yet available" placeholder tiles instead of imagery. **Root cause (measured, `ui-audit/diagnose-zoomdepth.mjs`):** Esri World Imagery is native to **z19**; `detectRetina` (added in B170 for sharpness) fetches **one zoom level higher** than the display zoom, so at deep zoom on a retina display the detail layer requested **z20** (168 requests), which Esri answers with the placeholder image. `maxNativeZoom:19` didn't account for the retina offset. **Fix:** drop the detail layer's `maxNativeZoom` by 1 on retina (`L.Browser.retina ? maxNative-1 : maxNative`), so the highest native fetch is z19 and beyond that it **upscales the deepest real imagery** instead of fetching nonexistent tiles — exactly the requested "clamp to deepest available zoom, scale the last good tiles up; never put placeholders over the plan." **Verified:** max tile zoom requested now **19** at both retina (DSF 2) and non-retina (DSF 1), **0** placeholder/empty responses (was 168). The coarse backfill (`maxNativeZoom:13`) was already clamping correctly. lint 0 · 257 tests · build green.

### B180 — Verify paving area excludes curb footprint `[Cost]` (bug)  *(arrived as "NEW-1"; minted **B180** — highest B# across both files was B179, so this is the real next free ID. Prerequisite for B181's paving-area trim.)*
`[x]` **Done 2026-06-19 (branch `claude/determined-feynman-l7c61b`). Investigated → confirmed correct by construction, and locked it in.** There was no pre-existing *cost* paving figure to fold curb into — the only paving area the app derived was the **impervious/SF** metric, where including the curb band is *correct* (curb is impervious surface), so that stays untouched. The new cost takeoff (B181) derives paving from the road's **face-of-curb-to-face-of-curb travel width** (`roadTravelWidth` = `min(w,h) − 2·curb`), which **excludes** the curb by definition: a 30′ FC-FC road yields 30′ of paving, never 31′. So the defect the brief warned about (folding the 6″ curb each side into paving) cannot occur — the cost math reads travel width, not the full element rectangle.
> **Locked against regression:** `roadQuantities` (in `lib/costTakeoff.js`) takes the FC-FC width explicitly and a unit test asserts a 30′ FC-FC × 1000′ barrier-curb road = exactly **30,000 SF (3,333.33 SY)** of paving with curb **outside** that width — so a future change that folded curb back in would fail the suite. **Live-confirmed in-browser:** a seeded 26′-cross road (FC-FC travel 25′) reads **1,611 SY** in the Yield panel (25 × 580 ÷ 9); had curb been folded in (26′) it would read 1,675 SY — the 1,611 proves the exclusion. 300 tests green, lint 0 errors, build green.

### B181 — Curb as a separate linear quantity type with curb-type attribute `[Cost]` (feature)  *(arrived as "NEW-2"; minted **B181** — next free ID after B180.)*
`[x]` **Done 2026-06-19 (branch `claude/determined-feynman-l7c61b`).** A road segment now derives **two independent, separately-priced quantities**:
> - **Paving** — an AREA item, **square yards (SY; 1 SY = 9 SF)** = length × FC-FC paving width ÷ 9. Width is stored/derived as **face-of-curb to face-of-curb** (the drivable surface), so a back-of-curb dimension can't silently inflate it.
> - **Curb** — a LINEAR item, **linear feet (LF)** = length × curbed-side count. **Both sides counted** (a 1,000′ road curbed both sides = 2,000 LF).
>
> **Curb carries a `curbType` attribute that selects its unit price:** `none` / **`barrier`** (vertical ~6″ lip, no gutter pan) / **`curb-gutter`** (curb + flat concrete gutter pan, typ. 18–24″). For **curb-and-gutter**, the gutter pan rides with the curb LF item, so **paving trims by the pan width on each curbed side** (`paving = FC-FC − panWidth × sides`); pan width is a property of the curb-and-gutter type (default 2.0′/24″, editable). Curb LF is split **barrier vs curb-and-gutter** in the rollup so each rides its own price.
> **Implementation:** new pure library `src/workspaces/site-planner/lib/costTakeoff.js` (`roadQuantities`, `costRollup`, curb-type readers) — no React, fully unit-tested (`test/costTakeoff.test.js`, 15 cases). Road-element fields are additive/optional (`curbType`, `curbedSides`, `panWidth`) and persist losslessly through the Site Model (els pass through untouched; no `SITE_MODEL_VERSION` bump). UI: a **"Curb & paving (cost)"** group in the road inspector (curb-type dropdown, curbed-sides Both/One, gutter-pan width, live per-segment SY/LF readout — inline editors only, no dialogs) and a collapsible **"Road cost (screening)"** section in the **Yield** panel (site-wide Paving SY + Curb LF by type, **user-supplied** unit-price inputs — blank by default, never a hardcoded $/LF — and a priced subtotal).
> **Hook left for later (not built):** `roadQuantities` tags each curb run `curbCategory: "linear"` so a future **radius/corner curb** category (often a higher unit price) can be broken out as its own priced line without reworking the rollup.
> **Verified:** 300 tests green (15 new), lint 0 errors, build green, lazy-chunk split intact. **Live headless (logged-out preview):** Yield → "Road cost (screening)" renders **Paving 1,611 SY · Curb · barrier 1,160 LF** with the FC-FC caveat and unit-price inputs, **zero page errors** (`ui-audit/verify-cost.mjs`).

### B176 — Jurisdictions overlay `[Site Planner]` (feature)  *(arrived as "NEW-1"; filed provisionally as B167, renumbered **B176** — concurrent `main` took B167–B175, so this is the real next free ID after B175.)*
`[x]` Toggleable boundary overlays so you can see, on the map, which districts a site sits in — county, city limits, city ETJ, and MUD/water districts. "See which sites are in a MUD around me." No school (per spec). Screening-grade, with a hard "boundary = has jurisdiction, NOT physically served/connected" caveat on every layer.
> ✅ **Done 2026-06-19** (branch `claude/busy-carson-1zuiuq`). New **`JURISDICTIONS`** overlay group in `lib/layers.js` + a dedicated **"Jurisdictions"** section in `components/LayerPanel.jsx` (with a one-line "has-jurisdiction ≠ serves utilities" caption). Four layers:
> - **County boundaries** (`esriFeature`) — reuses the **verified** TxDOT county FeatureServer already used by jurisdiction *identify* (B93), so the line you see is the line the identify reports.
> - **City limits** (`esriFeature`) — verified TxGIO city-boundaries layer (same source as identify).
> - **City ETJ (Houston region)** (`esriFeature`) — verified H-GAC 13-county ETJ layer (same source as identify); blank elsewhere (no statewide ETJ layer exists) — honest, not a bug.
> - **MUD / water districts** (`dynamic`) — **statewide TCEQ water-district boundaries** (the data behind TCEQ's public Water Districts Map Viewer, republished by HARC: `harcags.harcresearch.org/.../Boundaries/TCEQ_Water_Districts/MapServer`). Covers **Harris + Fort Bend + statewide** — not county-gated. (Initial cut shipped Fort-Bend-only; switched to the statewide TCEQ source same day when the owner pointed out TCEQ's mapper.) A `dynamic` image export renders via a CORS-exempt `<img>` even if the probe can't reach the host.
> All ride the existing probe + honest per-layer status + data-age (B96), so a dead endpoint shows a quiet "failed" dot, never wrong data. **Reuses verified endpoints (no new credentials, no parallel renderer); URLs imported from `lib/jurisdiction.js` so there's one source of truth (no drift).** lint 0 errors · 257 tests · build green. **Self-verified headless** (`gis-verify/jurisdictions-overlay-verify.mjs`): the Jurisdictions group + all four toggles render in the Layers panel and toggling County boundaries works with **zero JS errors**.
> **Live-render verification = V44 (handed off to a fresh session).** county/city/ETJ hosts (services.arcgis.com, feature.geographic.texas.gov) are reachable + return 200; the MUD host `harcresearch.org` was added to the env egress allowlist 2026-06-19 but is **not live in the session that shipped this** (already-running container keeps the old policy → still 403) — so a NEW session confirms the MUD tiles paint over a known Harris MUD parcel. If HARC ever proves unreliable in prod, alternates: City of Houston cohgis MUDs, or `services.twdb.texas.gov` water districts — a one-line URL swap in `JURISDICTIONS.jur_mud`.
> **Foundation for B177 (tax breakdown) + B178 (rate choropleth).** Supersedes the GIS-overlay "B" path of the old B165.

### B165 — Wire Harris County tax-rate source `[Site Planner]` (feature)  *(superseded)*
`[x]` **Superseded 2026-06-19 — split into B176–B179.** The original "wire a Harris tax rate" ask resolved into the A/B decision (browser GIS-overlay vs. backend exact fetch); the owner's actual goal ("see which sites are in a MUD; the tax statement lists every taxing entity") reframed it into four concrete items: **B176** jurisdictions overlay (shipped, above), **B177** parcel tax-breakdown panel (screening entities + rates), **B178** combined-rate choropleth, **B179** backend per-account exact fetch (the authoritative "A"). The B165 investigation (HCAD parcels carry no jurisdiction fields; official rate tables 403 automated clients → needs spatial overlay or backend) carries forward into B177/B179. Not deleted — kept here as the record; tracking now lives on B176–B179.

### B65 — White flashing on zoom/pan and general redraw `[Site Planner / map]` (bug)
`[x]` **Done 2026-06-19 (branch `claude/nice-heisenberg-r6lmf1`).** Zooming/panning a located site flashed (originally white) as the aerial blanked between redraws. **Root cause, measured not guessed** (headless probe `ui-audit/diagnose-flash.mjs`): the basemap view-lock effect called `map.setView(center, z, {animate:false})` on *every* planner `view` change; Leaflet's `setView`→`_resetView` fires `viewreset`, whose GridLayer handler `_resetAll` **wipes and reloads all tiles**, so each wheel step blanked the aerial for a frame (loaded tiles collapsed **22 → 2**), exposing the backdrop.
> **Fix shipped (two parts):**
> 1. **Mitigation (commit `cf26912`):** the basemap container backdrop was `PAL.paper` (`#f4f1ea`, near-white) → any gap read as a *bright* flash. Changed to a neutral mid-dark gray (`#3f3f3f`) **only while the aerial is on** (stays `PAL.paper` with the aerial off so the SVG-off background still matches). Bright flash → dim blink.
> 2. **Real fix — seamless transform-based zoom (commit `af10d1c`):** stop re-`setView`-ing on every frame. Hold Leaflet at a committed center/zoom and, for the small deltas of a live gesture, CSS-`transform` (translate+scale) the whole map container so the already-loaded tiles (and the shared FEMA/NWI overlay layers, which live on the same map) scale to track the gesture — no tile reload, no wipe, no flash. A debounced (180 ms) `setView` re-renders crisp once the gesture settles. First paint / resize / >1.5-zoom-level jumps commit immediately so the aerial is never scaled into a blurry mess. Transform math: `scale = getZoomScale(z, committed)`; translate so the desired center maps to the container centre (matches the SVG, which renders at true scale every frame), `transform-origin:0 0`. Container CSS transforms don't affect Leaflet's coordinate math (it uses cached `_size`/pixel-origin, and `clientWidth` is layout-box, unaffected by transform), so alignment is preserved.
> **Tested + rejected on the way:** flipping the map to `zoomAnimation/fadeAnimation:true` made it *worse* (gap frames 1→4, min loaded still 2) and reintroduces SVG-desync risk — not used.
> **Verified headless:** `diagnose-flash.mjs` now reports **0 gap frames** (min loaded 8, was 2); `ui-audit/verify-zoom-align.mjs` confirms the drawn building/parcel stay locked to the aerial through the transform and after the crisp commit (no jump). lint 0 errors · 257 tests · build green. **Live confirmation on planyr.io still nice-to-have** (a one-frame flash can't be captured headless), but the mechanism + alignment are proven. **Note:** Document Review's PDF viewer uses a different (canvas raster) renderer, not Leaflet — if zoom-flashing shows there too it's a separate fix, tracked if/when reported.
> **Follow-up 2026-06-19 (same branch) — kill the blank-backdrop reveal during the gesture.** After the flash fix shipped, the owner saw (screenshot) large flat cream patches while panning/zooming a project. Cause: the CSS-transformed basemap container (`geoWrapRef`) slid/scaled and exposed the canvas's `PAL.paper` (`wrapRef`) behind it. Fix (no new dependency): wrap the basemap in a **static clip box** (`inset:0`, `overflow:hidden`, dark `#3f3f3f` bg, never transformed) holding an **oversized inner map div** (`inset:-GEO_OVERSCAN`, `GEO_OVERSCAN = 320` px). The map now overhangs the viewport by 320 px on every side with `keepBuffer` tiles already loaded, so a normal pan/zoom transform reveals **real imagery**; anything beyond the overhang shows the **static dark backdrop**, never the cream page. The transform math is unchanged (it already uses `map.getSize()/2`, which adapts to the larger container, so basemap↔SVG alignment is preserved). Verified headless: `ui-audit/pan-reveal.mjs` (a hard mid-pan drag) now shows imagery edge-to-edge with no cream; `verify-zoom-align.mjs` still shows the geometry locked to the aerial through the transform + crisp commit. lint 0 errors · 257 tests · build green.
> **Follow-up 2 2026-06-19 (same branch) — kill the whole-screen "flash to black" on zoom-out.** Owner: zooming out from a building flashed the entire screen dark, even parts already loaded. Cause: the crisp re-render (`commit`) calls `map.setView`, whose Leaflet `viewreset`→`_resetAll` **removes ALL tiles and reloads them**, so for one frame the dark backdrop showed everywhere. Fix = **ghost-buffer the commit**: before the `setView`, clone the current basemap container (with its live CSS transform baked in) into a frozen overlay (`geoGhostRef`) pinned on top; the fresh tiles load underneath; on the tile layer's `load` event (or a 1.4 s fallback) the ghost is removed — so the old picture stays visible until the new one is ready and the wipe is never seen. Also dropped the old non-buffered ">1.5 levels → hard commit" branch; the crisp re-render now fires on settle (160 ms) OR once accumulated zoom drift passes ~0.75 levels, both **ghost-buffered** so a mid-gesture re-base no longer flashes. Verified headless (`ui-audit/zoomout-frames.mjs`, samples a continuous wheel zoom-out and measures the fraction of dark-backdrop pixels per frame): worst-case dark coverage **1.6 %** (UI edges only) — no full-screen black at any frame (was a brief whole-screen wipe). Alignment + crisp settle unchanged (`verify-zoom-align.mjs`). lint 0 errors · 257 tests · build green.
> **Follow-up 3 2026-06-19 (same branch) — fix the slow gray initial load (regression).** Owner: after the above, a freshly-opened site sat on the gray backdrop for ~10 s before the aerial appeared. Cause: the overscan (2.5× viewport) **×** detectRetina (2× density) ≈ **4× the tile bytes** on first paint, so on a real connection the detail tiles took ~10 s to cover (the sandbox is too fast to feel it — reproduced with Playwright CDP network throttling at ~1.2 Mbps: time-to-coverage **9.1 s**). Fix = a **coarse "instant" backfill layer**: a second `L.tileLayer` under the detail layer capped at `maxNativeZoom: 13` (a handful of large-area tiles, no detectRetina) that loads near-instantly and blankets the view with blurry imagery, then the heavy detail layer is added **only after the backfill paints** (`bf.once("load", addDetail)` + a 600 ms fallback) so its many retina tiles don't flood the connection and starve the coarse ones. Result (same 1.2 Mbps throttle): time-to-coverage **9.1 s → ~3 s** (blurry), detail then sharpens on top to the full 45 tiles; on a normal connection the coarse coverage is ~instant. No regression to the interactive fixes (zoom-out flash still 1.6 %, pan still full-imagery). `addDetail` guards against the aerial being toggled off mid-wait. lint 0 errors · 257 tests · build green. Harness: `ui-audit/initial-load.mjs` (optional `THROTTLE=1`).

### B175 — Parcel active/inactive checkbox in the Parcel menu `[Site Planner]` (feature)  *(arrived as "NEW-4"; filed provisionally as B170, renumbered **B175** — #152 (B167/B168) + #153 (B169–B171) collisions, so this is the real next free ID after B171. Core already shipped under B100; this filed + closed the residual.)*
`[x]` Done 2026-06-19 (branch `claude/optimistic-wozniak-4iltkx`). Per-parcel **Active checkbox** on each row of the left Parcel menu (checked = active; unchecked = inactive). B100 (2026-06-16) already shipped the underlying `active` flag, the `activeParcelsOf`/`siteSqft` active-only math, the dimmed-on-canvas render, and per-parcel persistence (Site Model `data` → Supabase). This item added the **literal checkbox affordance** the owner asked for (the prior control was a `✓ Active / ◯ Inactive` chip on the *selected* parcel only; now every row carries a checkbox, and the row + label dim with a "· inactive" tag) and **closed the merge-candidacy gap**: `toggleMerge` now refuses to add an inactive parcel to the combine set, and `mergeParcels` filters inactive parcels out of the chosen group — so an inactive parcel is excluded from yield **and** from the working merge, matching B100's intent. `SitePlanner.jsx` only; reuses the existing `toggleParcelActive` (pushes history + autosaves). **Self-verified headless (Chromium, logged-out preview):** two-parcel site → both checkboxes checked → unchecking parcel A dims its row + canvas polygon and stamps "· inactive"; the other parcel stays active. 257 tests green, lint 0 errors, build green.
> **Dedup note:** core was B100 — this is the affordance + merge-candidacy residual, not a re-implementation. No `SITE_MODEL_VERSION` bump (the `active` field already exists, defaulted-at-read).

### B174 — Reduce top app-chrome header height by 20% `[global UI / Shell]` (task)  *(arrived as "NEW-3"; filed provisionally as B169, renumbered **B174** — #152 + #153 collisions.)*
`[x]` Done 2026-06-19 (branch `claude/optimistic-wozniak-4iltkx`). `AppHeader.jsx` Row 1 height **44 → 35 px** (−20%); contents stay vertically centered (the row is `display:flex; alignItems:center`, unchanged). Row 2 (module tabs) stays 36 px. The reclaimed ~9 px flows to the map canvas (the body is `flex:1`). Distinct from B10 (which deduped the brand/module mark + added the switcher but left two physical rows). Browser-verified: header visibly thinner, tabs unchanged, no clipping of the logo/account controls.

### B173 — App pinned to the viewport; map scale bar always visible `[Site Planner / UI + Shell]` (bug)  *(arrived as "NEW-2"; filed provisionally as B168, renumbered **B173** — #152 + #153 collisions; next free after B171.)*
`[x]` Done 2026-06-19 (branch `claude/optimistic-wozniak-4iltkx`). Root cause: nothing guaranteed the app couldn't grow taller than the window, so the page itself could scroll and push the bottom-of-canvas scale bar below the fold (and the header off the top). Fix is structural, not a scale-bar move: `index.css` now sets `overflow:hidden` on `html, body, #root` and pins them to `100dvh` (dynamic viewport height — handles mobile browser chrome) via `@supports`; `Shell.jsx` adds a defensive `overflow:hidden` on the shell column. The in-canvas measurement-grade scale bar (bottom-right, `lib/sheetFurniture.js`, shared with print/export) sits a fixed 40 px above the canvas bottom, so once the page can't scroll it is **always** visible — no need to fork a second portal-mounted overlay (kept export parity; the owner's "bottom-left fixed overlay" was the suggested mechanism, the real need was "always visible," now met). **Self-verified headless at 1440×900 AND 1280×600:** `document.scrollHeight === innerHeight` (no page overflow) at both sizes, and the scale bar + north arrow render at the canvas bottom with both header rows visible simultaneously.
> **Dedup note:** distinct from completed **B144** (scale bar clipping/measurement-grade in print/export) — this was on-screen viewport layout.

### B172 — Module tabs read as inactive, not disabled `[global UI / Shell]` (bug)  *(arrived as "NEW-1"; filed provisionally as B167, renumbered **B172** — concurrent `main` took B167/B168 (#152, map items) and B169–B171 (#153, basemap-blur trio), so this is the real next free ID after B171.)*
`[x]` Done 2026-06-19 (branch `claude/optimistic-wozniak-4iltkx`). The inactive Schedule/Markup tabs were `rgba(255,255,255,0.30)` — low-opacity, which both killed contrast (read as disabled) and muddied the glyphs (the "fuzzy / low-res" feel). New `ModuleTab` component in `AppHeader.jsx`: inactive tabs are **full-opacity `#c9c3b4`** (muted-but-legible, AA on the `#14110e` bar), the **module accent reveals on hover** (Site `#1D9E75`, Schedule `#7F77DD`, Markup `#EF9F27`), and the **active** tab keeps the accent text + 2 px underline. Icons are crisp fixed-13 px SVG with `shapeRendering:geometricPrecision` (no bitmap scaling). Inactive font-weight bumped 400→500 for legibility; `aria-current="page"` on the active tab. Browser-verified: Schedule/Markup clearly readable and obviously clickable next to the active Site tab.
### B170 — Fix retina/HiDPI tile requests so the aerial basemap renders sharp `[Site Planner]` (bug)  *(arrived as "NEW-2"; filed provisionally as B168, renumbered **B170** — concurrent `main` (`claude/brave-galileo-wm8gti`) took B167 + B168 for unrelated map items, so this is the real next free ID; was gated on B169)*
`[x]` Gated on B169 (the diagnostic), which confirmed the dominant blur causes were **(a) HiDPI tiles never requested** and **(b) the planner's fractional zoom** — so the source-swap fix path (B171/NEW-3) was correctly NOT triggered, since the imagery source isn't the problem.
> ✅ **Done 2026-06-19** (branch `claude/awesome-darwin-7ui0qm`). Added **`detectRetina: true`** to **both** Esri World Imagery tile layers — the map finder (`MapFinder.jsx`, `L.tileLayer(bm.tiles, …)`) and the planner backdrop (`SitePlanner.jsx`, `GEO_BASEMAP`). On a HiDPI display (`devicePixelRatio > 1`) Leaflet now requests **one-zoom-higher native tiles and renders them at half size** (downsampled = crisp) instead of upscaling 1× tiles (= soft). This also sharpens the planner's *fractional* zoom (`zoomSnap:0`, driven by `ppfToZoom`) because it prefers downsampling a higher-zoom tile over upscaling a lower one. **Kept the Esri source** (per the NEW-2 constraint) and **did NOT touch the planner map's `zoomSnap:0`** — snapping that map to integer zoom would desync the aerial from the feet-space SVG (the deliberate ppf→zoom scale lock); `detectRetina` only changes which tiles are fetched + their display size, never the map's CRS zoom, so the lock is untouched. **Self-verified in a real headless browser** (`gis-verify/retina-basemap-verify.mjs`, simulating `deviceScaleFactor` 1 vs 2 over a Katy parcel): at standard density the map requests **z11** tiles; at retina density it requests **z12** (one level higher = 2× pixel density), with **0 failed/gray tiles** at either density (no coverage regression) — logged **VERIFICATION V43**. lint **0 errors** · **257 tests** · build green; `SitePlannerApp` lazy chunk intact. (The sharpness gain is invisible at the sandbox's `devicePixelRatio = 1`, so the *visible* improvement is best confirmed on Michael's own retina/4K display — but the higher-density tile request is proven engaged.)

### B169 — Diagnose basemap blur: retina/DPR vs. fractional zoom vs. source resolution `[Site Planner]` (task)  *(arrived as "NEW-1"; filed provisionally as B167, renumbered **B169** — concurrent `main` (`claude/brave-galileo-wm8gti`) took B167 + B168 for unrelated map items, so this is the real next free ID)*
`[x]` Diagnostic only — instrument + report the root cause of soft aerial imagery **before** any provider change (the fix paths are B170 + B171).
> ✅ **Done 2026-06-19** (branch `claude/awesome-darwin-7ui0qm`). Root cause established from the two tile-layer call sites (both the **Esri World Imagery** source, `server.arcgisonline.com/.../World_Imagery/MapServer/tile/{z}/{y}/{x}`) plus a live headless instrumentation:
> - **(a) Retina/HiDPI NOT requested — CONFIRMED, primary.** Neither `L.tileLayer` set `detectRetina` (nor any `@2x` / `tileSize:512`): the map finder (`MapFinder.jsx:291`) and the planner backdrop (`SitePlanner.jsx:1043`) both fetched **1× tiles**, which the browser then **upscales** on any `devicePixelRatio > 1` display (most modern laptops / 4K monitors) → soft. This is the most likely "it looks blurry on my screen" cause.
> - **(b) Fractional zoom — CONFIRMED for the planner backdrop.** The planner map runs **`zoomSnap: 0`** (`SitePlanner.jsx:1031`) and its zoom is driven **continuously** from `ppfToZoom(view.ppf, lat)`, so the raster is almost always displayed at a **fractional** zoom = interpolated = blurred (independent of DPR). The map finder uses Leaflet's default integer zoom, so it's not affected by (b) — only (a).
> - **(c) Genuinely low-res source — RULED OUT as primary.** Esri World Imagery is **native to z19** (`maxNativeZoom: 19`, ~0.3 m/px) with good Houston-metro coverage; both layers merely *display* past native (21/24), so the only "source" softness is upscaling past z19 at very tight zoom — not a stale/low-res source. Verified live (Katy parcel): tiles return **HTTP 200** with no gray/missing tiles at the working zooms.
> - **Live instrumentation:** `gis-verify/retina-basemap-verify.mjs` drove the headless map at `deviceScaleFactor` 1 vs 2 and recorded the **zoom level of every World_Imagery tile actually requested** + load status. Confirmed the pre-fix behavior (1× tiles, no DPR awareness) and gave the before/after baseline for B170.
> **Outcome → fix routing:** causes (a)+(b) are real and fixable **without** changing the imagery source → **B170** (add `detectRetina`, keep Esri, don't snap the planner's locked zoom). Cause (c) is not the issue, so **B171** (license-clean high-res source evaluation) stays **gated** — only revisit if B170 proves insufficient by eye on a real retina display. lint 0 errors · 257 tests · build green (no app-code change in this diagnostic item beyond the verify harness).

### B168 — Map-viewer project card: kill single-click delete, move to a right-click context menu `[Site Planner / map]` (bug + feature)  *(arrived as "NEW-2"; minted B168 — next free ID after B167)*
`[x]` Done 2026-06-19 (branch `claude/brave-galileo-wm8gti`). The "floating project card on the map canvas" is the **Your sites** panel (rendered over the map in `MapFinder.jsx`); each card row showed an inline ✕ that opened a delete dialog. Per the owner ("one accidental click destroys a project") the inline ✕ is **gone** — only the non-destructive **locate (⊕)** action remains on the row (still hover-revealed, B106). Delete now lives in the **right-click context menu**, folded into the existing status picker (B7), so there's **one** menu, not two: header (project name) → the five lifecycle states (`Pursuit → Active → On Hold → Complete → Dead`, current one checked) → a divider → a red **Delete project…** that routes through the **existing** `confirmDel` confirmation modal (no handler rewrite). The same menu opens from a **map marker/boundary** right-click too (shared `statusMenu` path), so "right-click any project → status or delete" is uniform. The menu is a `position:fixed` full-screen-backdrop overlay at `zIndex 4200` — above all map layers, no clipping — positioned at the cursor and clamped to the viewport (clamp bumped to fit the taller menu).
- **Dedup outcome:** the "Change status" half **reused** B7/B8's existing control (not rebuilt). B158 (the YOUR SITES *sidebar* right-click Rename/Delete menu) is **not yet built**; when it lands it should reuse this same menu rather than fork one — noted on B158. Rename wasn't requested here, so not added.
- **Self-verified** (headless Chromium, logged-out preview build, 2026-06-19, `ui-audit/verify-mapcard.mjs`): map loads with **0** inline `Delete site` buttons on the cards; right-clicking the JFK card opens the menu with the status options + **Delete project…**; screenshot confirms layout. lint **0 errors** (pre-existing warnings only) · **257 tests** · build green · `SitePlannerApp` lazy chunk intact. → **VERIFICATION V (B167/B168 row)**. ⏳ Signed-in re-confirm that Delete actually removes the site (self-test runs logged-out).

### B167 — Remove the "Drag to move the map" instructional bubble entirely `[Site Planner / map]` (task)  *(arrived as "NEW-1"; minted B167 — highest B# across both files was B166)*
`[x]` Done 2026-06-19 (branch `claude/brave-galileo-wm8gti`). The idle first-run bubble B105 had kept (the one-time, dismissible "Drag to move the map. Hit ＋ Select parcels to start adding lots." card, remembered in `localStorage planarfit:mapHintDismissed:v1`) is **removed entirely** — its state (`showMapHint`/`dismissMapHint`) and JSX are gone from `MapFinder.jsx`, and the now-dead localStorage key is no longer written. Per B21/B105 the bottom-left slot is shared, so the other two states were **left intact**: the **contextual selection guidance** (only while `selectMode` is on) and the **error toast** (only when `err`). Supersedes the kept-bubble part of B105 (B105 stays Done for removing the *persistent* card).
- **Self-verified** (headless Chromium, logged-out preview build, 2026-06-19): map finder loads with **no** "Drag to move the map" bubble. lint **0 errors** · **257 tests** · build green.

### B166 — Geometry check reads wrong county acreage (homesite carve-out, not total) `[Site Planner]` (bug)  *(arrived as "NEW-3"; minted B166)*
`[x]` **Repro (owner-reported):** parcel `0440110000065` — Appraisal panel + drawn boundary agree (17.00 AC vs 17.06 ac) but the geometry check screamed **"county 0.50 ac vs 17.06 ac (3,312% off)."** The 0.50 ac was the **HOMESITE carve-out** (Legal: "PT TR 4D (HOMESITE)"), not the total tract.
> ✅ **Done 2026-06-19** (branch `claude/busy-carson-1zuiuq`). Confirmed root cause against the **live HCAD parcels layer** field list: the record carries **both** `Acreage` (total) **and** `acreage_1` (a sub-acreage) — both match the acres regex, and `countyAcres()` used `keys.find(...)` which returned whichever came first (the homesite ~0.50). Fix in `SitePlanner.jsx` `countyAcres()`: collect **all** acreage-named fields, drop any whose name marks it a sub-acreage (`home_site|homestead|hs_|hmst|exempt|imprv|improv`), and take the **MAX** of what remains — the total tract is always the largest, so a homesite/ag/exemption carve-out can never win. County-agnostic (fixes the statewide carve-out pattern, not just this parcel). Distinct root cause from B75 (that was a m²-vs-acres unit misread; the TxGIO unit handling is untouched). lint 0 errors · 257 tests · build green.

### B164 — Move "Show setback line" toggle into the Parcel section `[Site Planner / UI]` (task)  *(arrived as "NEW-1"; minted B164)*
`[x]` The "Show setback line" toggle lived on the **Setup** tab, away from the parcel it acts on.
> ✅ **Done 2026-06-19** (branch `claude/busy-carson-1zuiuq`). The **Parcel panel already had** a setback-visibility toggle (Boundary › "Setbacks per edge" › the "Show" checkbox, same `settings.showSetback` state) — so the fix was to remove the **duplicate** from the Setup/Site-defaults tab (`SitePlanner.jsx` ~L6579) and relabel the parcel-panel one to the full **"Show setback line"** (with a tooltip). Net result: the control now lives only with the parcel object, exactly as asked; state/behavior unchanged. lint 0 errors · 257 tests · build green.

> **ID note (2026-06-16):** these three were filed/implemented as B93–B95, but a concurrent `main` (PRs #46/#51) had blindly taken B93–B100 for different items. Per the dedupe protocol they're **renumbered B101–B103** here (content is unique — no overlap with main's GIS/UI-overhaul items); the implementing commit message still says "B93–B95" (the provisional numbers). The colliding UI batch (PR #51) was separately renumbered **B104–B107**, so B93–B107 are now unique and gapless (GIS keeps B93–B96).

### B157 — Added-detention area label seats on the NEW ground, not the whole-pond centre `[Site Planner]` (feature)  *(arrived 2026-06-18 in chat — owner: when expanding an existing pond, show the added surface area + acreage at the centre of mass of the NEW area, because the whole-pond centroid often stays inside the existing basin. Refinement of B139/B140; filed provisionally as B151, renumbered **B157** — concurrent `main` took B150–B156 (easement tool, multipart-parcel + Delete-key bugs, markup hit-testing), so this is the real next free ID.)*
`[x]` Done 2026-06-18 (branch `claude/brave-noether-rwzkqk`). B140 stacked the "+added" increment as a third line on the pond's single centred label, so on a one-sided (or uniform) expansion the added figure floated over the EXISTING basin — the exact case the owner flagged, where the whole-pond centroid stays inside the old pond. Now the added detention area gets its OWN on-map label, seated on the **thickest part of the new ground**:
- **New `lib/pondGeom.js` (`addedAreaLabelPoint`, unit-tested):** the pole-of-inaccessibility of (expanded footprint − baseline footprint) via a coarse grid + local refine — the interior point of the new area farthest from any edge. Handles a one-sided strip (lands mid-strip), a uniform "push banks out" ring (lands in the band, never the centre), and a corner push; returns null on no-expansion / pure shrink / degenerate ring. Pure world-feet geometry (no React/DOM), so it unit-tests without a browser.
- **Label (`SitePlanner.jsx` element-label builder):** in expansion mode the centred label reads **"Existing Detention Pond"** + the existing area, re-anchored over the **existing basin** (baseline centroid); a second label titled **"Additional Detention"** + `"+X.XX ac · +Y sf"` is pushed at the added-region point (owner wording, 2026-06-18). A plain (non-expansion) pond label is unchanged ("Detention Pond" + area). The added area is the **footprint / top-down** area (the metric behind the owner's "20–25 % of the site" pond-coverage rule — NOT water-surface). Both ride B121's existing LOD/collision pool via a per-label id (`<id>` / `<id>#add`) — no parallel renderer. A net shrink / sub-`POND_ADD_MIN_SF` (50 sf) change falls back to the inline "+/−" increment so the delta still shows.
- **Verified (self, headless Chromium, logged-out preview build, 2026-06-18):** seeded a one-sided expansion + a concentric expansion; the existing-area label seats over the old basin and the "+added" label seats over the new ground (one-sided: to the right of the basin; concentric: out on the new ring, 183 px off the centre that sits inside the old pond), correct sf/ac, **zero console/page errors**. Re-verified after the owner wording change — labels read **"Existing Detention Pond"** and **"Additional Detention"** with the +ac/sf in place. lint **0 errors** (14 pre-existing warnings) · **257 tests** (6 in `test/pondGeom.test.js`) · build green · `SitePlannerApp` lazy chunk intact. Browser pass → **VERIFICATION V42**.
> Refines **B140** (stays Done as the acres+sf label); builds on **B139**'s `det.baseline`. No `SITE_MODEL_VERSION` bump — reads the existing persisted baseline, adds no field.

### B154 — Delete key intermittently needs two presses to remove a selected element `[Site Planner]` (bug)  *(arrived as "NEW-1"; filed provisionally as B151, renumbered **B154** — concurrent `main` took B150–B153 (easement tool) plus a second B151 (multipart parcel), so this is the real next free ID; the implementing commit references the bug as NEW-1)*
`[x]` Repro: click an element to select it, press Delete — sometimes it deletes on the first press, sometimes it takes two ("works when I was already on the canvas; fails when I came from a panel/button first"). **Root cause (confirmed, but NOT the report's first guess):** the keydown handler is **already** `window`-level — so it isn't a focus-scope problem. The real cause is a **stale closure via passive-effect re-binding:** `onKey` is created inside a `useEffect`, and although `sel`/`multi` are in its deps, the effect re-binds the listener **after** the commit/paint. A Delete pressed in the brief window right after a selecting click runs the *previously* bound `onKey`, which closed over the old `sel` (often `null`) → the guard `(sel || multi.length)` was false → no-op, no `preventDefault`; the next press (listener now re-bound) works. Under a heavy render (this is a ~6k-line component) the window widens enough to be hit intermittently.
> ✅ **Done 2026-06-18** (branch `claude/gallant-lovelace-82konu`). Added live `selRef`/`multiRef` mirrors (synced during render — a passive effect would lag the same window) and made the **Delete/Backspace branch read the refs**, and `deleteSel` read `selRef.current`/`multiRef.current`, so deletion always acts on the *current* selection no matter which listener instance fired — no second press. The handler stayed `window`-level (no scope change). Also **hardened the existing typing guard** — it already bailed when `document.activeElement` is INPUT/SELECT/TEXTAREA (so Delete never nuked canvas elements while editing a field); extended it to **`contentEditable`** too. The lone same-tick `setSel(x); deleteSel()` caller (the element context-menu Delete) is unaffected — the element is already selected when that menu opens, so the ref holds it. lint **0 errors** · **230 tests** · build green; `SitePlannerApp` lazy chunk intact. **Self-verified in a real headless browser** (built app): (A) select an element → one Delete removes it (2→1); (B) touch a tool/panel button, then select an element, then a **single immediate** Delete still removes exactly one — the reported flow, now first-press reliable; zero console errors. Logged as **VERIFICATION V40**.

### B150 — Easement drawing tool (core): first-class Easement object, input modes, attributes, editing, render, persistence `[Site Planner]` (feature)  *(arrived as "NEW-1"; minted B150 — next free ID after B149)*
`[x]` New first-class **Easement** object on the editable layer (immutable-backdrop model preserved): a `markups[]` entry `kind:"easement"` with a stable UUID and a display label derived from type + width (cosmetic, editable override). **Mode A — Centerline + width:** digitize a polyline centerline, enter total width → strip generated by offsetting the centerline ±width/2 with flat end caps; width is editable after creation and re-offsets the strip live. **Mode B — Boundary polygon:** draw a closed ring directly. Attributes panel (Element tab): type (Utility/PUE, Sanitary Sewer, Storm/Drainage, Water, Pipeline, Access/Ingress-Egress, Aerial/Overhead, Temporary Construction, Other), holder/beneficiary, width (auto-filled in mode A), recording reference, exclusive (y/n), status (existing/proposed), restrictsBuildings (default true), restrictsPaving (default false), notes. Editing: draggable centerline/polygon vertices (＋ add, Shift-click delete), move, delete, relabel; reuses the existing snap system + persistent toggle. Rendering: semi-transparent hatched fill color-coded by type, centerline shown for strip easements, proposed rendered dashed; per-easement area readout + total encumbered area in Yield. Persistence: Supabase, editable layer, private by default, RLS-protected.
> ✅ **Done (branch `claude/clever-franklin-7ds6kg`).** **Engine:** inspected the codebase first per the gate — an offset/buffer utility already existed (`bufferPolyline` in `lib/metesAndBounds.js`), so **no JSTS added**. Generalized it to be asymmetric-ready (`{leftW,rightW}`, default still symmetric ±w/2) so a future asymmetric-strip UI needs no geometry rework, and factored out a shared one-sided `offsetPolyline` primitive. **Domain logic** in new `lib/easements.js` (`EASEMENT_TYPES`, `easementLabel`, per-mode `deriveEasementRing`, `easementArea`). The **type dropdown uses the shared portal `AnchoredMenu`** so it can't render behind the rail/zoom rail. Easement is a markup → persists through the existing markups[] save path (Supabase, private, RLS) with no schema change. New rail "Easement" section (mode + default width/type via the portal menu). Self-verified in a headless browser (centerline + boundary draw, attributes panel auto-opens, portal type dropdown swaps type + relabels, vertex grips). **246 tests pass** (16 new in `test/easements.test.js`); lint 0 errors; build green.

### B151 — Import easement from a metes-and-bounds description `[Site Planner]` (feature)  *(arrived as "NEW-2"; minted B151)*
`[x]` Reuse the metes-and-bounds → polygon parser so the user can paste a legal description and spawn an Easement object (boundary geometry), attributes editable afterward. Depends on B150.
> ✅ **Done.** Reusability gate satisfied with **no parser duplication**: the parser already lives in `lib/metesAndBounds.js` (`parseCalls`/`callsToPath`) and already returns a polygon in the planner's coordinate frame (planar feet), so it wired straight in. Added a **"Plot as easement →"** button to the Title reader / metes-&-bounds modal; on the POB click it builds a `kind:"easement"` (mode B for a closed tract; a `mbWidth` corridor strip for an open traverse) via the shared `makeEasement`/`commitEasement` path, then auto-selects it with the attributes panel open. Self-verified in a headless browser (closed traverse → easement placed, panel shown, 0 page errors).

### B152 — Offset-from-parcel-edge easement mode `[Site Planner]` (feature)  *(arrived as "NEW-3"; minted B152)*
`[x]` Third input mode: select one or more contiguous edges of an active parcel and enter a width → one-sided strip parallel to and inset from the boundary (the "10′ utility easement adjacent to the property line" pattern), with corner mitering where the run turns. Depends on B150; shares the offset engine. Designed so the same machinery is reusable for a future setback/building-line tool (no setbacks built now).
> ✅ **Done.** `buildParcelEdgeStrip` (easements.js) orders the chosen edges into one contiguous run (rejects non-contiguous / whole-boundary selections), auto-picks the **inward** side (offset toward the parcel interior), and miters turns via the shared `offsetPolyline`. Clickable edge hit-targets render over each active parcel in this mode; a floating **"Create easement ⏎"** bar (mirrors the merge-parcels bar) confirms. The stored easement keeps the edge-run as its editable centerline + `offsetSide`, so width edits and vertex drags re-derive correctly. Self-verified headless (the edge hit-target is the topmost element at the click; strip created).

### B153 — Easement exclusion-zone hook for buildable area / yield `[Site Planner]` (task)  *(arrived as "NEW-4"; minted B153)*
`[x]` Expose each easement's geometry + restrictsBuildings/restrictsPaving flags as exclusion zones the yield/buildable-area calc can consume (subtract restrictsBuildings from buildable footprint, restrictsPaving from pavable area). Depends on B150. Integrate if a buildable calc exists; else expose the data + hook cleanly.
> ✅ **Done — data + hook exposed (no premature integration).** Confirmed current state first: there is **no** buildable-area/verdict engine yet (`developableArea()` was a stub; the Yield panel reports drawn areas, not a buildable envelope), so subtracting from existing numbers would be wrong. Added `exclusionZonesOf(m)` (each easement's ring + restriction flags; defaults block buildings, allow paving), made `developableArea(m)` surface them so the future engine consumes them with no rework, added `easementsOf`, and folded `"easement"` into `EASEMENT_KINDS` (so `constraintsOf` includes them). Surfaced encumbered totals (gross / restricts-buildings / restricts-paving) in the Yield panel. Covered by `test/easements.test.js`.

### B147 — Markup/shape tools: Bluebeam-style editing (rotate + resize boxes, vertex-edit lines/polylines/polygons) `[Site Planner]` (feature)  *(arrived 2026-06-17 in chat: "fix the rectangle and markup tools … make them more like bluebeam, not being able to rotate a rectangle is wild"; minted B147 — next free after B146)*
`[x]` The neutral markup shapes (drawn with the Line / Rectangle / Ellipse / Polygon / Polyline tools) could only be **moved, restyled, or deleted** — selecting one just recoloured it, with **no handles**. You couldn't rotate or resize a rectangle/ellipse, or reshape a line/polyline/polygon. Meanwhile elements have resize/rotate grips and measures/parcels have vertex editing, so markups were the odd one out. **Owner's headline: "not being able to rotate a rectangle is wild."**
> ✅ **Done 2026-06-17** (branch `claude/gallant-lovelace-82konu`). Added Bluebeam-style geometry editing to the markups[] layer, **reusing the existing element/measure transform machinery** (no parallel system):
> - **Rect / ellipse:** on select (Select tool), 4 corner + 4 edge **resize grips** + a **rotate handle** above the top edge — mirrors the element handles. Resize is computed in the shape's own rotated frame (opposite side stays fixed); rotate spins about the centre, snapping to 15° when Snap is on, free (whole degrees) when off. The `rot` field already existed on rect/ellipse (rendered via `transform=rotate`), so this is purely the missing UI. New `mkResize`/`mkRotate` drag modes + `startMarkupResize`/`startMarkupRotate`.
> - **Line / polyline / polygon:** draggable **vertex dots**; **＋** on edge midpoints (polyline/polygon) to insert a point; **Shift-click** a dot to delete one (keeps the kind's minimum — 2 for line/polyline, 3 for polygon). New `mkVertex` drag mode + `startMarkupVertex`/`addMarkupVertex`, mirroring the measure-vertex handlers (B141). Shared helpers `mkPts`/`setMkPts` normalise line `{a,b}` ↔ point arrays.
> - **Properties panel:** rect/ellipse gain numeric **Width / Height** and **Rotation°** fields (precise entry; kept out of `mkStyle` via a separate `setSelMarkupGeom` so new shapes don't inherit a past size/angle), plus a one-line editing hint per shape.
> - **Scope:** only the 5 neutral annotation kinds get grips; **semantic markups stay move-only** (utilRoute / traced / infwater / encumbrance carry derived geometry — corridor/pad/centerline/calls — that hand-editing would desync). Locked markups show no grips.
> lint **0 errors** · **225 tests** · build green; `SitePlannerApp` lazy chunk intact. **Self-verified in a real headless browser** (drove the built app): drew a rectangle → corner+edge grips + rotate handle appear → dragged a corner (300→410 px) and the rotate handle (rotate(0)→rotate(53)); drew a 3-point polyline → 3 vertex grips + 2 ＋ handles → dragged the middle vertex and the path updated; zero errors from the change. Logged as **VERIFICATION V38**.

### B144 — Scale bar clips off-frame and isn't measurement-grade in print/export `[Site Planner]` (bug)  *(arrived as "NEW-1"; filed provisionally as B141, renumbered **B144** — concurrent `main` PR #114 had already taken B140–B142 and PR #117 took B143, so this is the real next free ID; the implementing commit still references the provisional B141/B142)*
`[x]` Repro: Print/PDF (and PNG export) of the map view → the scale bar's max label ("500") clipped at the right frame edge, the "0" floated above the bar instead of under the start tick, and the segments read as noise rather than a measured ruler. **Root cause (confirmed):** the export reused the *on-screen* scale bar's **screen-pixel** sizing (`scaleBarFt.px ≈ 120 px` + fixed `28`/`30` px offsets) and screenshotted it into a differently-sized export frame, so it overflowed; labels were also drawn *above* the bar with no plate.
> ✅ **Done 2026-06-17** (branch `claude/gallant-lovelace-82konu`). New pure module **`lib/sheetFurniture.js`** draws the furniture **sized in OUTPUT units and anchored to the export FRAME**, not the screen: everything is a fraction of `refS = min(frame w,h)`, so it lands at a fixed physical size on the page (on a letter sheet the short side ≈ 8.5 in → safe-area inset ≈ 0.38 in). The scale bar now **snaps to a round real-world distance** (`pickScaleBar`, from 50/100/200/250/500/1000 ft + smaller/larger steps for extreme zoom) that best fills a target width band **without exceeding a hard ceiling** (so it can't clip); **alternating black/white segments**; **tick marks at 0 / midpoint / max with the numbers centered directly under their ticks**; a **"FEET"** unit label; on a **translucent legibility plate** so it reads over light pavement or dark canopy. The graphic bar stays the source of truth (its length encodes the distance, so it survives image rescaling — no "1 in = X ft" text scale). Recomputes on zoom. `buildExportSvg` (`SitePlanner.jsx`) composites it into the print/PNG viewBox; the old screen-pixel furniture block + the now-unused `scaleBarFt` were removed. **Also applied on-screen** (owner asked to "fix the site planyr app too"): the live canvas uses the same primitives via `buildScreenFurnitureSvg` (viewport-anchored, fixed modest size) so the app matches the print. **Workspace confirmed Site Planner only** — Document Review has no print furniture (nothing shared to fix twice). lint **0 errors** · **222 tests** (18 new in `test/sheetFurniture.test.js`: round-distance snap, no-clip ceiling, safe-area anchoring, plate/label/FEET render, no compass rose, screen variant) · build green; `SitePlannerApp` lazy chunk intact. **Self-verified in a real headless browser** (drove the built app: drew buildings → File ▾ → Print / pick frame → Print, captured the actual print page; also captured a PNG export and the on-screen corners) — scale bar fully inside the frame, round **0 / 125 / 250 FEET**, no clipping. Logged as **VERIFICATION V34**. Shared root cause + fix with **B145**.

### B145 — North arrow oversized and illegible in print/export `[Site Planner]` (bug)  *(arrived as "NEW-2"; filed provisionally as B142, renumbered **B145** after the same concurrent-`main` collision as B144)*
`[x]` Repro: Print/PDF (and PNG export) of the map view → the north arrow was oversized for the frame, sat directly on the imagery (illegible over dark canopy), and used generic placeholder styling. **Same root cause as B144** — drawn as a screen-pixel overlay (a fixed-radius circle + triangle) screenshotted into a differently-sized export frame, rather than composited in output units.
> ✅ **Done 2026-06-17** (branch `claude/gallant-lovelace-82konu`, same change as B144). The north arrow is now a **clean filled arrow + "N" on the same translucent legibility plate** as the scale bar (`northArrowPlate` in `lib/sheetFurniture.js`), a **modest fixed ~0.5 in tall in the export** (`0.06·refS`), **anchored top-left with a fixed margin inside the safe area** so it never clips, and it **rotates to true map bearing** (points straight up on the planner's north-up canvas; `bearingDeg` param wired for any future view rotation). **No ornate compass rose** — a simple arrow, the convention for engineering exhibits (a unit test asserts no `<circle>`). Verified together with B144 (same browser run, **V34**) — print page shows a modest, legible arrow top-left on its plate.
### B143 — Text box edits like Bluebeam: Enter = newline, click away / Esc to finish `[Site Planner]` (bug) — follow-up to B142
`[x]` Done 2026-06-17 (branch `claude/practical-noether-ln1f46`). Owner: starting a text box then clicking out "won't let me" exit. Two causes, both fixed to match Bluebeam's Text Box markup (researched against the Bluebeam Revu docs — "finish by selecting away or pressing Esc"; a text box is multi-line):
- **Click-out was trapped at ANY distance:** the canvas pointerdown `preventDefault`s the textarea blur, so `onBlur`→`commitEditCallout` never fired and you stayed in the editor even clicking 400 px away. Fix: a full-canvas transparent catcher rendered under the inline editor (`pointerEvents:"all"`, commits on pointerdown) — clicking anywhere outside the box now finishes it.
- **Enter committed (single-line behavior):** Enter now inserts a **newline**; you finish by clicking away or Esc (Esc keeps the text). Placeholder updated to "Type; click away or Esc to finish".
- Empty box is still removed when you click away without typing (matches Bluebeam).
- Verified live (headless): Enter → "Line1\nLine2" (still editing); click away +80 px → finishes + commits two lines; Esc → finishes keeping the text; empty box removed on click-away; zero console errors. lint 0 errors · build green · tests pass. Browser pass → **VERIFICATION V33**.
> Cross-ref: completes the text-box UX started in **B142** (selectable across the whole box) — together they make text boxes select / edit / finish like Bluebeam.

### B140 — On-map pond footprint area labels (existing & expansion) `[Site Planner]` (feature)
`[x]` Done 2026-06-17 (branch `claude/practical-noether-ln1f46`). Pond map labels now show **acres + sf** (was sf only): `<ac> ac · <sf> sf` (e.g. "4.78 ac · 208,161 sf"). In expansion mode (B139) the label becomes a 3-line stack — name / existing **TOTAL** `ac · sf` / **+added** increment `+x.xx ac · +y sf` — so the new total is inferable (mirrors the panel's "+ac-ft"). A focused change in the element-label builder (`SitePlanner.jsx`), reusing `polyArea`/`f2`/`SQFT_PER_ACRE` and B139's `det.baseline`; rides the existing **B121** label LOD/collision engine (no parallel renderer). Verified live (headless): plain pond "3.60 ac · 156,730 sf"; expanded "+1.18 ac · +51,599 sf". **Deferred (polish, noted):** the per-view hide toggle + leader-line for too-small ponds — the LOD engine already drops labels that don't fit, so neither blocks. Browser pass → **VERIFICATION V31**.

### B141 — Bluebeam-style editable vertices on the measurement tools `[Site Planner]` (feature)
`[x]` Done 2026-06-17 (branch `claude/practical-noether-ln1f46`). Area / polyline / line measurements are now control-point editable: **drag** a vertex to reshape, click a **＋** on an edge to insert one, **Shift-click** a dot to delete (area keeps ≥3 pts, line/polyline ≥2); area + perimeter recompute live. New `startMeasureVertex`/`addMeasureVertex` mirror the polygon-element vertex handlers; a `measureVertex` drag mode updates `measures[].pts`; legacy `{a,b}` records migrate to `{mode,pts}` on first edit. Handles render only when the measure is selected (Select tool), styled like the parcel/element handles. Verified live (headless): selecting an area shows the handles; one vertex drag changed it 166,530 → 257,958 sf. Reuses the established vertex-edit pattern (consistent with Doc Review's shape-aware select). Browser pass → **VERIFICATION V31**.

### B142 — Text box not selectable on click `[Site Planner]` (bug)
`[x]` Done 2026-06-17 (branch `claude/practical-noether-ln1f46`). The callout/text-box rect now sets `pointerEvents="all"`, so the **whole box** is clickable even when its fill is none/transparent (previously a no-fill box caught clicks only on the painted area / thin border — the likely "click does nothing" cause). Filled boxes unchanged; `deleteSel` already removes a selected callout. **Honest note:** a DEFAULT-fill text box already selected fine in headless testing (create → deselect → click reselects, the "Text box" panel opens, Delete removes) — I could **not** reproduce the reported "nothing happens" for a normal box, so this is a hardening fix for the no/transparent-fill case that matches Doc Review's shape-aware hit test. If it still recurs on a specific box, reopen with that box's fill setting + exact steps. Resize handles for text boxes were intentionally not added (they auto-size to their text). Browser pass → **VERIFICATION V31**.

### B132 — Detention pond expansion: lock an existing pond as a baseline, then read the storage gained `[Site Planner]` (feature)
`[x]` Done 2026-06-17 (branch `claude/practical-noether-ln1f46`). Owner workflow: trace an existing on-site detention pond, lock it as the baseline, then enlarge it (drag banks out and/or deepen) to read the **added detention** in acre-feet. Built entirely on the existing pond machinery — no parallel store, no new schema bump.
- **Panel** (`SitePlanner.jsx`, pond branch of the selected-element inspector): the **Detention storage** section gains a **"Lock as existing pond"** button. Locking snapshots the current footprint ring (world feet: `selEl.points` or `elCorners`) **plus its own depth/freeboard/slope** into `el.det.baseline`, so the comparison is frozen and apples-to-apples. Once locked, an **"Expansion vs. existing"** box shows **Existing** / **Proposed** storage and a live **"Storage gained +X.XX ac-ft"** (green) — or **"Storage lost"** (red) when shrunk below baseline — plus the cubic-feet delta. **Clear** removes the baseline (keeps depth/freeboard/slope).
- **Increase = `detentionStorage(proposed) − detentionStorage(baseline)`** — the *same* prismoidal/taper function for both sides, so the delta can't drift from a method mismatch. Reflects both footprint growth and any depth/slope change.
- **Canvas ghost** (`renderElPx`): the locked baseline ring draws as a faint **dashed outline** under the pond so the expansion is visible. Handled for both the irregular-polygon branch (drawn in world feet directly) and the rotated-rectangle branch (a counter-rotated `<g>` undoes the element's `rotate(el.rot)` so the world-feet ghost lands true). `pointerEvents:none` so it never interferes with selection/drag.
- **Persistence:** `baseline` rides inside the already-persisted `el.det` (additive, lossless) — survives save/reload, no `SITE_MODEL_VERSION` bump. Honest framing kept: "screening only — confirm with your civil engineer."
- **Scope (owner-chosen):** "just the increase." Deliberately **excluded** for now (offered, declined): a required-vs-provided "is it enough?" check against added impervious, and a solver ("grow this much to hit a target"). Easy follow-ons if wanted.
- **Verified:** lint 0 errors (14 pre-existing warnings, none in new code) · **191 tests pass** · build green · lazy-chunk split intact (`SitePlannerApp` own chunk). Browser click-through logged as **V25** in `VERIFICATION.md`.

### B123 — Restructure building label + make square footage persist on zoom-out `[Site Planner]` (feature)
`[x]` Done 2026-06-16 (branch `claude/determined-brown-bf86l7`). The building label is now a 4-line **level-of-detail stack** built on B121's shared engine — name → square footage → "(incl. N bump-outs)" → dimensions — so the **square footage survives zoom-out far longer** instead of vanishing early.
- **New ordering + wording** (`lib/labelLayout.js` `buildingLabelLines`, unit-tested): square footage is now its **own line** (was crammed onto the sf line with the bump-out note), the bump-out note is a **separate parenthetical line shown only when the building has bump-outs**, and its wording changed from "+N bump-outs" to **"(incl. N bump-outs)"**. `SitePlanner.jsx`'s building-label branch builds the stack via that helper; every other element label is unchanged.
- **Drop order = dimensions → parenthetical → square footage → name.** The stack is ordered highest-priority-first and B121's `fitLines`/`layoutLabels` drop from the end, so dimensions go first on zoom-out and square footage outlives them (it drops only at extreme zoom-out). Reuses B121's pool — not a parallel renderer, as the item required.
- Poly-drawn buildings keep their existing name/sf/ac label; only rect buildings get the 4-line stack.
- **Tested:** `test/labelLayout.test.js` adds a `buildingLabelLines` case (exact order, singular/plural + conditional parenthetical, and a `fitLines` assertion proving sf outlives dimensions). lint 0 errors · **174 tests pass** · build green · lazy split intact. Browser-verify logged as **V21** in `VERIFICATION.md`. Completes the B122 → B121 → B123 building-label arc.

### B122 — Auto-number buildings with contiguous renumber-on-delete `[Site Planner]` (feature)
`[x]` Done 2026-06-16 (branch `claude/determined-brown-bf86l7`). Each standalone building now shows a sequential **"Building N"** label, numbered by placement order and contiguous (1…N) at all times. Deliberately **render-derived, not stored**:
- **New selectors** in `lib/siteModel.js`: `isBuilding(el)` (a `type:"building"` element that is NOT an attached dog-ear/bump-out — those carry `dogEar` + `noLabel`) and `buildingNumbers(els)` → `Map(id → N)` assigned in `els` (placement) order. The number is recomputed from list position every render, so **deleting a building renumbers the rest 1…N in one pass** with no stored field to migrate; a lone building is "Building 1".
- **Label** (`SitePlanner.jsx` element-label render): a standalone building's first label line is now `Building ${n}` (was the bare type name "Building"); every other element label is unchanged. Dog-ears/bump-outs are excluded (already `noLabel:true`), so the parent's bump-out tally ("… (+2 bump-outs)") is untouched.
- **Identity unchanged + safe:** the "stable internal ID, separate from the number" requirement was already met — every element has a persisted, collision-seeded `el.id` (`uid()` + `ensureIdAbove` on load), and every cross-reference (`attachedTo` for parking/trailers/dog-ears) already binds to `el.id`, never a display number. So a renumber can't silently re-point anything. No schema change, no migration, no `SITE_MODEL_VERSION` bump.
- **Tested:** `test/siteModel.test.js` adds a `buildingNumbers` case (contiguous 1…N by placement order, dog-ears excluded, renumber-on-delete, single→"Building 1", null-safe). lint 0 errors · **139 tests pass** · build green · lazy-chunk split intact. Browser-verify logged as **V13** in `VERIFICATION.md`.
> Unblocks **B123** (building-label restructure): it reuses `buildingNumbers`/`isBuilding` for label Line 1, then folds the rest of the 4-line stack into **B121**'s shared label collision/LOD engine.

### B119 — Parking orientation flipped: first bay must sit against the building `[Site Planner]` (bug)  *(arrived as "NEW-1")*
`[x]` Done 2026-06-16 (branch `claude/youthful-heisenberg-9w64n4`). **Root cause:** `addParkingRowSide` created building-attached parking with `cfg:{ flipDepth:true }`. The strip's inner (local `y=0`) edge sits against the wall, and `flipDepth` mirrors `carStalls`'s layout so the **drive aisle** landed on that inner edge — i.e. the aisle hugged the building and stalls were pushed outward (the inverse of the spec). **Fix (one line):** drop `flipDepth` for building-attached parking. `carStalls` now lays the first stall row at the building edge with the drive aisle outboard, and `growParking` already grows on the away-from-host edge — so the field grows **outward, away from the building**, exactly as specced. The per-field "Drive aisle on the far side" toggle (B59) still flips it by choice; existing saved parking keeps its stored `cfg` (only newly-added side parking changes). Lint 0-errors, 152 tests pass, build green. Browser-confirm logged as **V17** in `VERIFICATION.md`.

### B120 — Parking +/− stepping: bay-and-aisle increment sequence `[Site Planner]` (bug → was dedupe to B69)  *(arrived as "NEW-2")*
`[x]` Done 2026-06-16 (branch `claude/youthful-heisenberg-9w64n4`). The +/− depth sequence (`42 / 60 / 102 / 120 / 162 / 180 …`; double-load an aisle before adding a new one) was already implemented + unit-verified as **B69** (`parkDepthForRows`/`parkRowsForDepth`/`growParking`) — the math never needed changing. It only *looked* wrong because of **B119**'s flipped orientation: the numerically-correct steps grew toward/into the building. With B119 fixed (stalls hug the wall, growth is outward), the steps now visibly grow in the correct direction. No code change here beyond the B119 fix. Browser-confirm logged as **V17** in `VERIFICATION.md`.

### B128 — Split tool: correctly cut concave parcels (general multi-piece straight cut) `[Site Planner]` (bug → enhancement)  *(minted B127 on-branch; renumbered B128 after `main`'s #85 concurrently took B127 for the rail/header dropdown fix — the implementing commit still says "B127")*
`[x]` Done 2026-06-17 (branch `claude/inspiring-gates-qrhziq`). Closes the QA marathon's last "substantive bug still present" hand-off and supersedes the long-deferred **B31**.
- **The data-corruption fear was already mitigated, and that was verified, not assumed.** The hand-off carried an `[S2/HIGH]` "Split corrupts area on concave parcels — saved silently, no error." That premise was **stale** (written against pre-#48 code): `performSplit` already had a concave-cut **backstop guard** (area-conservation + per-piece self-intersection check, from the B74–B92 batch) that *refuses* a bad cut with a warning instead of saving corrupted geometry. Confirmed by replicating the live `splitPolygon` + guard and fuzzing **320k random concave cuts** (combs/L/U/stars) against a Monte-Carlo point-membership oracle: **zero** wrong splits slipped past the guard; every 4/6/8/10/12-crossing cut was rejected. So there was no silent `siteSqft`/FAR/coverage corruption to fix.
- **The real remaining gap was capability, not safety.** Because the old `splitPolygon` only paired the two extreme-`t` crossings, a straight cut that crosses a concave lot **4+ times** (which honestly yields **k+1** pieces for `2k` crossings — e.g. 3 for a U cut across both prongs) could only ever produce 2 overlapping/omitting "halves," so the guard always **refused** it. Net user-visible behavior: "the Split tool won't cut an L/U/notched lot through its reentrant part."
- **Fix — a correct general slice.** Extracted the parcel-split geometry out of `SitePlanner.jsx` into a new pure, tested module **`lib/polygonSplit.js`**: `splitPolygonByLine(points,A,B)` classifies each vertex to a binary side (guarantees an even crossing count — no odd-parity tangency bug), inserts a crossing node on every sign-change edge, **pairs ALL crossings even–odd along the cut line** (each pair is an interior chord), and traces every resulting piece (walk a boundary arc → hop the interior chord to its partner → continue), dropping zero-area slivers. Returns the natural set of pieces (`>=2`) or `null`. The bent-polyline cut moved over too as `splitPolygonByPath` (unchanged logic). `performSplit` now replaces the one parcel with **N** parcels and keeps the area/self-intersection guard as a backstop, so the result can never be worse than before (a rare near-vertex-grazing degenerate sliver still self-intersects → still refused).
- **UX change to note:** a straight cut through a notched lot now creates **all** the real pieces (e.g. 3 parcels for a U-shaped lot), instead of declining. Convex and 2-crossing concave cuts are byte-for-byte the same two pieces as before.
- **Validation:** new `test/polygonSplit.test.js` (11 cases: convex halving, L 2-crossing, **U → 3 pieces with exact areas + membership**, 3-tooth comb → 4 pieces, line-misses/degenerate → null, bent-path cut, and a 1,500-cut randomized area+membership property check). Standalone prototype passed the 300k-trial oracle (0 area / 0 membership failures; ~0.003% near-degenerate self-int → safely refused). Full suite **163 tests** (was 152, +11) · lint **0 errors** · build green · lazy-chunk split intact. Owner approved shipping; merged to `main` via PR.

### B116 — Measurement tools (Length / Polylength / Area) in Site Planner `[Site Planner]` (feature → already implemented)  *(arrived as "NEW-1"; committed provisionally as B115 — renumbered B116 after main's #72 took B115 for "revisit keyboard shortcuts")*
`[x]` Done 2026-06-16 (branch `claude/youthful-heisenberg-9w64n4`). **Dedupe outcome: already implemented — this is the "the app already had them" case the PHASE E spec and this very task anticipated.** Site Planner's `measure` tool already ships all three calibration-aware modes; nothing was rebuilt. Evidence in `SitePlanner.jsx`:
- **All three modes present + surfaced:** the **Measure** rail button + `▾` mode menu (Length / Polylength / Area) — `measureMode` state persisted to `localStorage` (`planarfit:measureMode`); two-click **Length**, running-path **Polylength** (accumulate points), closed-polygon **Area** (click the first dot or double-click to close). Click handling, `finishMeasure`, `selectMeasure`, delete, and the in-progress draft preview all exist and work.
- **Area label already matches the spec exactly:** renders `"<sf> sf · <ac> ac · <perim>′ perim"` (e.g. `12,300 sf · 0.28 ac · 462′ perim`) — perimeter closes the loop; Length/Polylength label the running feet.
- **Calibration-aware, as specced:** labels read from `calibrationState`; when an underlay is `uncalibrated` they render **amber (`#b45309`) with a `⚠`** and the rail shows the **"Underlay isn't calibrated — distances may be wrong"** banner (same behavior as Document Review / PHASE E). A drawn/georef/calibrated plan labels in the normal accent.
- **Own editable layer; export behavior consistent:** measurements live in their own `measures[]` layer (migrated `{a,b}`→`{mode,pts}`), persisted in the Site Model. They export alongside callouts/markups — consistent with the existing annotation architecture (the only `data-export="skip"` users are transient chrome: grid, basemap, scale bar, north arrow, zoom/layers UI, draft previews). Reuse note: the Document Review takeoff math (`takeoff.js`) and the Site Planner helpers (`polyArea`/`centroid`/`pathLen`) compute the same geometry — doc-review in page-units×calibration, the planner natively in feet (the shared EPSG:2278 spine) — so no port was needed.
- **Finish UX:** double-click **or Enter** commits (shared `finishActiveDrawing`, per B103); **Esc cancels** the draft. The task floated "Esc to end" — deliberately **not** adopted, since Esc-cancels-the-draft is the universal convention used by every other multi-point tool in the app; double-click/Enter already satisfy "click points → end".
- **Only change made (label alignment):** the mode display names were **Line / Polyline / Area**; renamed to **Length / Polylength / Area** — Bluebeam's own terms (Planyr is "Bluebeam-style") and the names this task uses, which also **disambiguates the measurement from the markup "Polyline" tool** (`mpolyline`, ⇧N) that shared the word. Label-only via a module-level `MEASURE_MODES`/`measureModeLabel`; the internal `measureMode` values (`line`/`polyline`/`area`) are untouched, so `localStorage` and all logic are unaffected. Lint 0-errors, 138 tests pass, build green. Browser click-through logged as **V12** in `VERIFICATION.md`.

### B114 — Snapping always on; can't be suppressed during a drag `[Site Planner]` (bug)  *(arrived as "NEW-1")*
`[x]` Done 2026-06-16 (branch `claude/epic-dirac-3ljl49`). Dragging an element (e.g. a road) near a neighbour flush-snapped it to that neighbour's edge with no per-move escape, so you couldn't drop it at a deliberate offset (a 15-ft buffer behind trailer parking). Root cause: the existing "Alt-drop to place free" only skipped the *bond* at drop (`onUp`'s `!e.altKey` guard); the element had already been moved to the flush-snapped position during `onMove`, so Alt never placed it freely — and there was no keyboard toggle. Fix:
- **Hold-to-suppress (the standard CAD pattern):** a new `altSnapOffRef`, re-armed from `e.altKey` on every `onBgDown`/`onMove`, makes the live snap = `settings.snap && !altSnapOffRef.current`. `snap()`/`snapPt()` read it (so grid snap, draw, vertex, and placement all honour Alt), and a per-frame `snapOn` gates the inline paths the core helper doesn't cover — the ambient neighbour flush-snap (`edgeSnapCenter`), resize/edge-resize-to-grid, and rotate-to-15°. Holding Alt bypasses ALL of them for that one move and re-enables on release. Shift-drag (deliberate align+bond) is unaffected.
- **Toggle + shortcut + default OFF:** the toolbar button stays, plus a new **S** shortcut and the Site-defaults checkbox toggle snap; "off" fully disables every snap path above. Per owner request snap now **defaults OFF (free movement)** and is a **global drafting preference** (a tool mode), not a per-site attribute: it lives in `localStorage` (`planarfit:snap`, default off) via `loadSnapPref`/`saveSnapPref`/`setSnap`, seeded at the settings `useState` and preserved across JSON import, so the per-site saved `snap` is ignored on load (every existing site started free, regardless of its baked-in `snap:true`). Turning it on persists across sites/reloads until turned off. (`settings.snap` is still the field every read site uses — it just mirrors the pref now.)
- **Visible state:** the toolbar pill now reads `Snap 10′` ↔ `Snap off` (green/grey dot, `aria-pressed`), the Site-defaults checkbox is relabeled "Snap to grid & neighbours (S)", the Select-tool hint says "hold Alt to bypass snap & place freely • toggle snap with S", and the shortcuts panel gains `S → Toggle snap` and `Alt drag → Bypass snap`. Build green; lazy-chunk split intact. **Browser-verify** logged in VERIFICATION.md.

### B101 — Remove the element-type color legend from the Setup/defaults page `[Site Planner / Setup]` (task)
`[x]` Done 2026-06-16 (branch `claude/blissful-babbage-liboyn`; filed provisionally as B93). Confirmed display-only first: element colors are *defined* in the "Element default colors" section (the color pickers writing `settings.typeStyles` via `setTypeStyle`) and in `TYPE`/`typeStyle`, which the canvas reads — the legend was just a swatch+label key. Removed the legend block only; color definitions untouched.

### B102 — Roads defaults: show "ft" units + rename "Travel widths" → "Road widths" `[Site Planner / Setup — Roads]` (bug)
`[x]` Done 2026-06-16 (filed provisionally as B94). Labels are now "Curb width (ft)" and "Road widths (ft)" (matching the panel's other `(ft)` labels). Label-only change — the persisted `roadCurb`/`roadWidths` setting keys and the travel+curb geometry are unchanged. (The per-element "Travel width (ft)" field on a *selected road* is left as-is — there it's accurately the drivable width excluding curbs.)

### B103 — Uniform polygon completion across ALL drawing tools `[Site Planner / drawing]` (bug)
`[x]` Done 2026-06-16 (filed provisionally as B95). One shared `finishActiveDrawing()` now drives BOTH Enter and double-click (`onBgDouble`), so finish/auto-close is identical across parcel, every area element (building/paving/parking/trailer/pond/sidewalk/landscape + custom polygons), measure, split, markup poly/polyline, and trace — Enter previously had no handler for parcel / element-polygon / mpolygon. Added `removeLastVertex()` so Backspace/Delete undoes the last placed vertex mid-draw (before the delete-selection fallback); Esc-cancel already existed. Road free-draw still finishes as an open path (no forced closing segment). Each finisher keeps its own min-point guard, so Enter/double-click no-ops instead of cancelling a too-short draft; `draftPoly`/`draftElPoly` added to the keydown effect deps so the handler reads the current draft.

### B1 — Sign-up form: missing fields `[auth]` (bug)
`[x]` Sign-up must collect **First name**, **Last name**, and **Organization/Company**. These fields are currently absent.
> Done 2026-06-14 (PR #10). First/Last/Organization added to the sign-up form, stored in Supabase `user_metadata` via `signUp` `options.data`; the signed-in account view now greets by name + org.

### B2 — Account control: placement & styling `[global UI]` (bug)
`[x]` Move the sign-in / sign-out control to the **top-right** (standard web-app convention) and clean up its styling. Current placement and formatting are rough.
> Done 2026-06-14 (PR #10). Account control moved to the shell header (top-right, global across workspaces) and restyled as an avatar+name pill; the old bottom-right pill was removed and the auth modal now lives in the shell.

### B3 — Brand spelling: global `[global]` (bug)
`[x]` Product name is **Planyr**. Replace both wrong spellings everywhere they appear: "Planar" (a-r) and "Planner" (normal spelling). Includes the module label: **"Site Planner" → "Site Planyr."**
> Done 2026-06-14 (rename PR #7 + PR #10 + PR #11). All user-facing brand marks are now "Planyr" / "Site Planyr": shell brand, module tab, map + planner headers, export/print labels, export-error text, and the tax label ("Combined rate" → "total tax rate"). The last stray "Site Planner" (Document Review help text) was fixed in PR #11. **Exception:** the browser-tab title keeps "Industrial Site Planner" as a descriptive tagline, per the owner's call (see "item two" decision, 2026-06-14). Internal identifiers, folder/file paths, and the `planarfit:*` localStorage cache keys are intentionally unchanged (not user-facing; renaming the cache keys would orphan saved data).

### B4 — Parcel selection doesn't reset on return to map `[Site Planner]` (bug)
`[x]` Repro: select parcels → "plan these N parcels" → work in the planner → back out to map. Currently the parcels stay selected and the map is still in select-parcels mode. Expected: returning to the map **clears the committed parcels** and **exits select-parcels mode** back to the normal map state.
> Done 2026-06-14 (PR #10). MapFinder now clears highlights/selection and exits select-parcels mode via an effect that fires whenever the map becomes visible again.

### B5 — Multi-select + merge parcels `[Site Planner]` (feature)
`[x]`
- **Shift+click multi-select** (Bluebeam-style): click a parcel, hold Shift, click others to add to the selection; Shift+clicking a selected parcel de-selects it (toggle).
- With multiple selected, two ways to merge into one: (a) right-click → **"Merge parcels"** context menu, and (b) a **"Merge parcels"** button in the right-hand parcel tool panel.
- **Terminology locked to "Merge"** — remove any "Combine/Combined" labels project-wide.
- This is a *working* merge on the editable layer (geometry for test-fit/yield), **not** a recorded legal consolidation. Button copy must not imply a county filing.
> Done 2026-06-14 (PR #10). Shift-click toggles multi-select in the Select tool; merge via a right-click context menu and a "Merge parcels" button in the parcel panel; the old Combine tool was removed and all user-facing labels say "Merge". Copy states it's a working merge for test-fit, not a recorded consolidation. (Residual non-user-facing leftovers: an unused `combine` icon-glyph key and two code comments — cosmetic, no user impact.)

### B6 — Auto-parking returns wrong dimension after sidewalk resize + delete `[Site Planner]` (bug)
`[x]` Repro: place building → add 30-ft sidewalk → parking within sidewalk → expand sidewalk to 30 ft → delete parking → click "add parking" → tool produced a ~135-ft parking field. Expected: "add parking" drops a **fixed-depth** field every time, regardless of surrounding geometry. Fixed depth is the spec, so the bug is that **something overrides the depth constant with a measured value**. Trace where the parking field's depth is assigned; confirm it can't be contaminated by adjacent or just-deleted geometry (the resized sidewalk and the gap left by the deleted parking are prime suspects).
> Done 2026-06-14 (PR #10). `addParkingRowSide` now offsets by the sidewalk's true *thickness* (`swThick`, which resolves the correct axis) instead of a raw `w`/`h` read that could pick up the sidewalk's *run* (~wall length); the row depth stays the fixed `stallDepth + aisle` constant and can't be contaminated by adjacent or just-deleted geometry.

### B7 — Project lifecycle status on map markers `[Site Planner / map]` (feature)
`[x]` Right-click a project marker to set its status. Lifecycle: **Pursuit → Active → On Hold → Complete → Dead** (see B8 for the full state set and visuals). Core of this item:
- Right-click marker → menu of states, current one checked. Mirror the same control in the project detail panel if one exists.
- **Status is a real field** on the project record in Supabase — persists across devices, stays private per the existing row-level security (each user sees only their own).
- **New projects default to Pursuit.**
- Companion features: **filter by status** (toggle each state on/off — "hide Complete" and "hide Dead" will be the common ones), a **legend**, and **pipeline counts** (e.g., "4 Pursuit · 6 Active · 2 On Hold · 11 Complete · 5 Dead").
> Done 2026-06-14 (this PR — B7–B12 batch). `status` added to the Site Model (`SITE_MODEL_VERSION` 2→3; new sites default to "pursuit"; pre-feature records migrate to "active"), exposed via a `statusOf` selector with ordered `STATUSES` + `STATUS_META`. It persists inside the model `data` — i.e. the `sites.data` jsonb in Supabase, so it syncs across devices and stays private under the existing RLS, with **no DB migration / no dedicated column**. Set a site's status by right-clicking its map marker **or** a saved-sites list row (current state checked); applied to the whole site group. Companion legend + per-state show/hide filters + live pipeline counts live in the saved-sites panel.

### B8 — Lifecycle states & marker visuals `[Site Planner / map]` (feature)
`[x]` The five states and how each marker reads. Use two redundant cues — **color + shape/glyph** — so status stays legible for colorblind users and on a busy basemap:
- **Pursuit** — amber, dashed ring (tentative, uncommitted).
- **Active** — green, solid (the live work; should grab the eye).
- **On Hold** — slate/indigo blue, solid ring, pause "‖" glyph (alive but paused; revivable). Lean slate/indigo rather than sky-blue so it doesn't vanish over blue water on the basemap.
- **Complete** — gray, muted, small check ✓ (done; recedes).
- **Dead** — hollow gray, dimmest, faint ✕, no fill (didn't proceed; kept for the record). **Not red** — red reads as urgent/alert; a dead deal is inert.
- Live (green) should pop; both Complete and Dead recede into the background, with shape/glyph telling them apart.
> Done 2026-06-14 (this PR — B7–B12 batch). Markers carry two redundant cues exactly per spec: Pursuit amber/dashed-ring, Active green/solid (pops), On Hold slate-indigo/pause-‖, Complete gray/muted-check-✓ (recedes), Dead hollow-gray/faint-✕/no-fill (recedes; not red). Glyph distinguishes Complete from Dead.

### B9 — Wire this backlog into CLAUDE.md `[repo / workflow]` (task)
`[x]` Add a line to the repo's `CLAUDE.md` telling future runs to check `BACKLOG.md` on every run — work the **🔲 Open** section, skip **✅ Done**. This closes the loop so the backlog is consulted automatically, without anyone having to point at it each time.
> Done 2026-06-14 (PR #11). `CLAUDE.md` opens with a callout pointing every run at `BACKLOG.md` (work 🔲 Open, skip ✅ Done), disambiguated from the ops "Deferred / maintenance backlog" section near the end of that file.

### B10 — Consolidate the two header rows into one bar with a module switcher `[global UI]` (feature)
`[x]` The Site Planner currently stacks **two** header rows that repeat information: row 1 is `Planyr | Site Planner | Document Review` (top-level module tabs); row 2 is `‹ Map | Site Planyr | Houston ColdPort ▾ › Plan 1 ▾ | undo/redo | Snap 10' | Saved ✓ | File ▾`. The brand and the active module name both appear twice.
- **Collapse to a single top bar.** Switch modules via a **product-switcher dropdown** — the standard multi-product pattern (think Atlassian/Google Workspace): brand logo + current module name on the far left with a chevron; clicking it reveals the other modules (Site Planner, Document Review, and later Project Scheduling, Cost Estimating). This replaces the row of flat tabs.
- **Recommended one-row layout:** `[▾ Planyr · Site Planyr]  ‹ Map › Houston ColdPort ▾ › Plan 1 ▾   ·   [undo] [redo] [fit] · Snap 10' · Saved/Cloud ✓ · [account] · File ▾`. (Map → Project → Plan reads as a breadcrumb — the trail showing where you are in the hierarchy.)
- Honor **B3** spelling here: row 1 still shows the old "Site Planner" — the rebuilt bar must use **Planyr / Site Planyr**.
- Removing the county selector (see **B11**) frees additional header space, reinforcing this consolidation.
> Done 2026-06-14 (this PR — B7–B12 batch). The shell's flat module tabs are now a **product-switcher dropdown** (brand + current module + chevron → menu built from the `WORKSPACES` registry, active one marked); the duplicated brand/module mark was removed from the planner header so brand/module appears exactly once. Global account control unchanged. Note: the shell bar and the planner context bar remain two physical rows — the dedup + switcher are the substance of B10; a single physical row is a later polish.

### B11 — Remove the county pre-selection gate; click anywhere, auto-resolve the county `[Site Planner / map]` (feature)
`[x]` Today the user must pick a county (e.g., Harris vs. Fort Bend) *before* clicking a parcel. Remove that gate — the user should be able to pan/click **anywhere** and select any parcel without choosing a county first; the unnecessary upfront choice just adds friction.
- Parcel geometry is published per **county appraisal district (CAD)** — the county agency that maintains the parcel map and ownership data (Harris County Appraisal District, Fort Bend Central Appraisal District, etc.). So "click anywhere" means the backend must **resolve which county the clicked point falls in** (point-in-county lookup) and route the parcel query to that county's CAD service automatically.
- Handle the **county-boundary case** gracefully (a click near a line, or a multi-parcel selection that straddles two counties → query both services and merge results).
- This also removes the county dropdown from the header, feeding **B10**.
> Done 2026-06-14 (this PR — B7–B12 batch). County gate removed. The map resolves every configured CAD county's parcel layer up front; a click is auto-routed via `candidateCountiesForPoint` (coarse per-county bbox pre-filter, padded so borders overlap → a straddle click queries both) + `identifyParcelAcross` (parallel point-identify across candidates, first hit wins, a down county is skipped not fatal, no match fails gracefully — no parcel, no crash). The selected parcel's resolved county rides into the planner hand-off so the saved site still records its county. The county dropdown is gone from the map header. **Interim:** the Layers panel defaults to the Harris/Houston jurisdiction (same as the prior app default) so the Houston utility overlays stay available with no county picked — per-area jurisdiction resolution is a future refinement.

### B12 — Rename the "start / import" entry point to describe what it does `[Site Planner]` (task)
`[x]` There's a control (current label rendered to me as "skip-point" — **needs confirmation of exact label/location**) that is the on-ramp where you **bring in a survey or a site and start editing** — i.e., drop a surveyed drawing or pick a site and begin a new working session on the editable layer above it. The current wording doesn't communicate that.
- **Goal:** rename it to plainly describe the action. Candidates, depending on exactly what it does: **"New Site,"** **"Start New,"** **"Import & Edit,"** **"Drop a Survey,"** or **"New Plan."** If it's specifically the *alternative* to selecting existing parcels (start from a survey/blank instead), **"Start Blank"** or **"Start from Survey"** fit.
- **`[?]` resolved:** the control was confirmed to be the map's **"Skip — blank canvas"** button, which opens an empty (un-located) planner — it does NOT import a survey today.
> Done 2026-06-14 (this PR — B7–B12 batch). Relabeled the "Skip — blank canvas" button to **"Start blank"** with a tooltip ("Start a new blank plan without selecting parcels"). Behavior unchanged (opens an empty planner; no survey-import was added — that would be a separate, larger item if wanted).

### B14 — Document Review project library: link reviews/files to Projects + browseable folders `[Document Review]` (feature)
`[x]` Link saved Document Review reviews/files to the existing **Project/Site** records (the entity that anchors Site Planner plans, e.g. "Houston ColdPort") and give them a browseable folder structure. Browser-only + existing Supabase.
- **Lifecycle status** reused from the Site Model (pursuit/active/onhold/complete/dead — already shipped in B7/B8); files stay attached regardless of status.
- Each review/file carries a **project link, discipline** (Survey / Civil / Architectural / Landscape / Environmental / CAD / Geotech / Other), **item/type, revision, and date**; the name defaults to **"\<Project\> - \<Item\> - YYYY.MM.DD"** (each piece editable).
- **Storage** organized as `<uid>/project-<id>/<discipline>/<file>` (uid first so the existing private-per-user RLS is unchanged).
- **File-explorer drawer:** project (+ status badge, editable) → discipline folder → files newest-first → click to open in the viewer/stitcher; **drag-drop** a PDF onto a project or discipline to file it.
> Done 2026-06-15 (PR #15, branch `doc-review/project-library`). New `doc_reviews` index columns (`project_id`/`item`/`revision`/`doc_date`) + a `ProjectLibrary` drawer + a project/discipline filing UI in the Reviews menu, all in the doc-review workspace. Projects + their lifecycle status are read from (and written back to) the Site Planner `sites` — one source of truth, no parallel store. Status is NOT re-added (it already exists per B7/B8). Migration `src/workspaces/doc-review/db/project_library.sql`. `upsertReview`/`listReviews` fall back to the core columns if that migration hasn't run yet, so saving never regresses.

### B66 — Top-left Site Planyr dropdown renders behind the lower header row `[global UI]` (bug)
`[x]` Clicking the top-left module (product) switcher opened its dropdown *behind* the planner's context bar / map header, so it couldn't be used. Root cause: the Shell `<header>` (which holds the dropdown) and `<main>` (which holds the workspace) were siblings, neither establishing a stacking context, so the dropdown menu (`zIndex:50`) competed in the **root** stacking context against the planner context bar (`SitePlanner.jsx`, `zIndex:60`) and the map header (`MapFinder.jsx`, `zIndex:1000`) — and lost.
> Done 2026-06-15 (branch `site-planner/b64-b66-runtime-ui-fixes`). Fixed in `src/app/Shell.jsx`: `<main>` now establishes a stacking-context floor (`position:relative; zIndex:0`), capping ALL workspace content (the context bar's 60, the map header's 1000, in-workspace modals) beneath it, and `<header>` is lifted above it (`position:relative; zIndex:1`). The global switcher dropdown now always paints over the workspace regardless of the lower bars' internal z-indexes; the shell-level AuthPanel (`zIndex:5000`) still sits above everything. More robust than just bumping the menu's number (which would depend on the exact stacking tree). Build passes.

### B68 — Opening a project with saved elements renders a blank screen (`cfgOf is not defined`) `[Site Planner]` (bug) — crash, high
`[x]` Opening a project that has saved layout elements (e.g. **"Schiel Road"**) blanked the whole page; the console showed `ReferenceError: cfgOf is not defined`, thrown inside the `els.map` during render — so empty/new projects (iterating over nothing) didn't crash, which is why only populated projects blanked. **Root cause:** `cfgOf` is a component-scoped helper (`SitePlanner.jsx:2449`, it closes over `settings`), but it was *also* called at `SitePlanner.jsx:5237` inside `renderElPx` — a **module-level** function that receives `settings` as a parameter — where the component-scoped `cfgOf` is not in scope. It shipped because it compiles fine; a build-only CI check can't see an unresolved identifier at runtime. This is exactly the "disjoint-but-interdependent concurrent merge" failure mode called out in **B63**: a parking `cfgOf` call site (added with B29/B59 in PR #22) landed in a scope where the helper isn't visible, and the clean merge + green build hid it. (Arrived from a parallel chat as provisional "NEW-1"; minted **B68** here — B67 is reserved by the in-flight PR #23 — and deduped: no prior blank-screen item existed.)
> Done 2026-06-15 (branch `claude/loving-knuth-6n43df`, pending PR). Three parts. **(1) Fix:** `renderElPx` now resolves `cfgOf` locally from its `settings` param (identical semantics to the component's), so the parking branch no longer references an out-of-scope symbol. **(2) Build-time guard (the real prevention):** added a minimal ESLint flat config whose headline rule is `no-undef`, run as a CI step before the build (`npm run lint`) — verified it flags the exact bug (`5237:38 'cfgOf' is not defined`) on the pre-fix file, so this whole class of dangling reference now fails the build instead of shipping a blank page. **(3) Hardening:** each lazy workspace in `Shell.jsx` is wrapped in a React `ErrorBoundary` (keyed by workspace id), so a future render crash shows a legible "couldn't load this view" fallback (Try again / Reload) instead of unmounting to a blank page — the shell, the switcher, and the other workspace keep working. `lint` / `test` (30 pass) / `build` all green.
