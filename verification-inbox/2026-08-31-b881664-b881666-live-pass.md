# Verification inbox — 2026-08-31 — B881664 (×2) / B881666 (V496864 / V496865)

**Why this file exists.** `Blocker: real-data` items in `VERIFICATION.md` can only be closed by a
live pass on Michael's signed-in browser — this session's own sandbox cannot sign in. This directory
is the missing pipe (B825232). Append-only. A session drains it into `VERIFICATION.md`, moves fully
passed items to `VERIFICATION-DONE.md`, and marks the entry drained here with the PR number.

Run on Michael's real signed-in account against the deployed bundle serving `planyr.io` right after
PR #1241 (`5e37ed82` on `main`), specifically against `SitePlannerApp-CSMS-RZm.js`. Method: fully
reload the tab, wait 20s to settle, install a `hashchange` recorder, click the control under test
once, watch 9s.

---

## V496864 — B881664 — FAIL (recurrence, ×2): the bounce is WIDER than before, not fixed

**NEW-1 (Dashboard breadcrumb bounce) is NOT closed.** From **Schedule**: hash reaches "#/"
immediately, then at **t=+2.1s** navigates to `#/project/<id>/site` — Dashboard never usable. From
**Library**: hash reaches "#/" at t=+61ms, then at **t=+2.4s** navigates to
`#/project/smsdrvzr9gzx/site` — **Richfield, which is NOT the project the owner was in** (Goose
Creek, `smqfy48tlk9j`). It resumes the most recently touched site, not the current one. The same
bounce was reproduced (in a faster loop, timing not separately logged) from **Review** (`/markup`)
and **Notes**.

**Comparison to the pre-#1241 baseline, reported honestly as a hypothesis, not a fact:** an earlier
pass (pre-#1241) measured this same click landing on "#/" and STAYING, with the dashboard rendered,
from Review/Library/Notes — only Schedule bounced. That earlier pass used 3.5–4s waits; this pass's
bounce fires at ~2.1–2.4s, so the earlier pass's clean result on Review/Library/Notes cannot be fully
trusted (it should have caught a bounce at that latency, but a difference in exact repro conditions
between passes cannot be ruled out either). Reported as the working hypothesis — "wider than before"
— not asserted as certain.

**The discriminator offered:** clicking the **logo** goes to "#/" and does NOT bounce — sat on the
dashboard 6s, and the NEW-2 filter test (below) ran fine there. Investigated by the session that
received this report: the logo and the "Dashboard" breadcrumb call the literal identical
`onDashboard` handler (confirmed by direct code reading of `AppHeader.jsx` / `ProjectBreadcrumb.jsx`)
— so this was NOT a real per-control difference. The actual cause was a mount-freshness artifact
(the logo click in this session's own sequence happened AFTER `SitePlannerApp` had already mounted
once, during the earlier NEW-2 filter check) — see the B881664 (×2) BACKLOG.md entry for the full
root-cause chain this discriminator led to.

**Steps not separately itemized this pass** (see V496864's steps 1–4 in `VERIFICATION.md`): the
"wait ~10s with no click" negative control (step 3) and the "boot lands directly on Dashboard,
should still resume last-open plan" legitimate-case control (step 4) were not separately re-driven
— this pass focused on reproducing/measuring the bounce itself.

---

## NEW-2 — B881665 (dashboard map pin filter) — PASS, already `Verify: sandbox` / Done, no open V#

Filter "goose" → list 1/28, pins drop 29→2. Filter "zzzzz" → list 0/28, pins drop to 1 (the single
Comp marker, which a site-name filter correctly should not remove — dispatcher explicitly declined to
file that as a defect). Clearing restores 28/29. No action needed in `VERIFICATION.md` — this item
had no pending live-verify entry to drain.

---

## V496865 — B881666 (switcher dedupe) — PASS

**Step verified:** opening the project switcher shows the current project (Goose Creek) exactly
ONCE, marked "current," with the recents list continuing correctly into Richfield / Woods Road
(no other duplicate reported anywhere in the list). Dispatcher's own verdict: "CLOSED, verified by
me on production." Which specific tab (Site vs. Schedule) and whether a mid-session multi-project
switch (V496865's steps 2–4) were separately exercised was not itemized in the report beyond "the
open dropdown" — recorded here exactly as reported, not embellished.

---

## NEW-4 — B881667 (unknown sub-route false stale-build banner) — PASS, already `Verify: sandbox` / Done, no open V#

`#/project/<id>/review` (an unknown alias — the real route is `/markup`) no longer shows the false
"your tab is out of date" banner and now redirects to the project's default tab. Dispatcher's own
verdict: "Correct." No action needed in `VERIFICATION.md` — this item had no pending live-verify
entry to drain.

---

> **Drained → `VERIFICATION.md` in this session (2026-08-31), shipped as a follow-up PR.**
> V496864 updated in place with this FAIL evidence and RE-PARKED against the new async-resume fix
> (`resumeTargetAfterSignIn`) — still open, now naming the async signed-in leg as the specific
> outstanding check. V496865 marked PASSED and moved to `VERIFICATION-DONE.md`; B881666 moved to
> `BACKLOG-DONE.md`. NEW-2/NEW-4 needed no `VERIFICATION.md` action (already `Verify: sandbox` /
> Done). Draining is a transcription, not a rubber stamp: V496864 is NOT marked passed on any
> account — it is recorded as a genuine live-verify FAILURE that drove a new fix.
