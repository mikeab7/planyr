# `docs/` — on-demand reference (read only when the task touches it)

These files were split out of `CLAUDE.md` on 2026-07-02 so the always-loaded handoff stays
lean. `CLAUDE.md` (repo root) is still the core every session reads to orient; pull one of
these up only when your task needs it.

**Top level holds only durable references someone opens on purpose** — a normative doc, a
schema/rules reference, a generated status table, or a "read this before touching X" pre-read.
Everything else lives in a subfolder (see "Where a new document goes" below).

| File | What's in it | Read when |
|------|--------------|-----------|
| `DESIGN.md` | **The design bible — the one normative doc.** Color tokens, radius/spacing/type scales, primitive specs (Button/ToggleChip/IconButton/Field/Section/MenuItem), the canvas/SVG-vs-chrome boundary, and the CI drift guard's rules. | You're adding or changing ANY UI — read it before writing the first line of markup (see root `CLAUDE.md`). |
| `DESIGN-TOKENS.md` | The B809906 audit *behind* the token values `DESIGN.md` cites (738-button audit numbers, the two still-open judgment calls) — not a second bible, superseded as "the design bible" by `DESIGN.md` itself. | You need the provenance/audit numbers behind a specific token, not the rules. |
| `DATA.md` | The data bible — entity ownership, the numbered invariants (each with its proving test), the one-answer functions (`isOwnWrite`, `assemblyIntegrity`, `nameAuthority`, …), the failure contract, the destructive-action contract. | You're touching persistence, sync, undo, or delete — read it FIRST (see root `CLAUDE.md`). |
| `NOTES-CARRY-FORWARD.md` | The Notes module's own pre-read: the instrument traps that have each produced a confident false finding about working Notes code, the one fixture that finds real bugs, storage keys + the standing SQL health check, the verification bar, the recurring bug families. | You're working on `src/workspaces/notes/` — read it FIRST, before the module's own `CLAUDE.md` pointer (see root `CLAUDE.md`). |
| `UI-INVENTORY.md` | Generated, computed-style inventory of every deviating control actually painting today (NEW-3) — regenerated by CI, never hand-edited. | Checking whether a specific control is on-scale, or what's still drifting. |
| `VISUAL-REGRESSION.md` | Generated visual-regression baseline table — coverage, the masking/tolerance policy, the approval workflow, current baselines — built from `ui-audit/visual-baselines/manifest.json`, never hand-edited. | Checking visual-regression coverage, or approving a new baseline. |
| `SHIPPED.md` | Full catalog of shipped-and-verified features (Site Planner, Supabase backend, multi-workspace foundation, Document Review) + the retired GitHub-Pages deploy note. | You need the history/detail of a specific already-built feature. |
| `ROADMAP.md` | The two-track roadmap — Site Planner maturation + Document Review buildout — and deferred/decision-gated items. | Planning new feature work. |
| `REFERENCE.md` | Deep implementation detail: Site Model schema, map-layer/GIS plumbing, Supabase DDL/RLS, Document Review persistence internals, the sandbox Playwright quirk. | You're editing that subsystem and need the internals. |
| `PERSISTENCE.md` | Persistence internals reference + pointer to the manual test script. | You're editing save/load/sync code and need the mechanism, not just `DATA.md`'s rules. |

## Subfolders

| Folder | What goes here |
|--------|-----------------|
| `docs/perf/` | Performance investigation writeups (`PERF-*.md`) and their close peers (e.g. `CAPTURE-PIPE.md`, a telemetry-capture proof cited alongside `PERF-LAYERS.md`) — dated, one-off measurement reports. Superseded/refuted findings stay in place with a correction banner (a measurement is a historical fact); they are not deleted or rewritten. |
| `docs/incidents/` | One-off incident write-ups, Cowork/session handoffs, and audit writeups — a postmortem of a specific dated outage or bug, a brief handed to a teammate to finish one job, a critique/audit pass over one feature. Not a durable reference anyone opens routinely; opened to look up what happened on a specific past item. |
| `docs/testing/` | Manual test/audit scripts a human or a Cowork session runs by hand (persistence test script, UI audit walkthrough). |
| `docs/sessions/` | One-off session artifacts: `COWORK-BRIEF-*` / `COWORK-RESULT-*`, `HANDOFF-*`, external-tool briefs. |
| `docs/archive/` | `BACKLOG-DONE.md` / `VERIFICATION-DONE.md` — write-only historical archives. Never read except to look up one specific past ID; never hand-edited for anything other than the archiving process itself. |

## Token discipline (why this split exists)

The costly habit was loading big files just to find one small thing. Two rules keep it cheap:

1. **`CLAUDE.md` is the lean core** — standing rules, how to talk to Michael, the architecture
   spine, KEY DECISIONS, workflow. Bulky detail lives here in `docs/` and is read on demand.
2. **Grep the big tracking files; don't slurp them.** `BACKLOG.md`, `VERIFICATION.md`, and the
   two `*-DONE.md` archives are hundreds of KB. To find work, `Grep` the headings
   (`^### B` / `^### V`) then `Read` only the one block you'll act on. The `*-DONE.md` archives
   are write-only — never read them except to look up one specific past ID.

## Where a new document goes (2026-09-04 — the `docs/` reorganization)

Repo root keeps only what's opened by name: `README.md`, `CLAUDE.md`, `MAP.md`, `BACKLOG.md`,
`BACKLOG_OPEN.md`, `VERIFICATION.md`, `OWNER-TODO.md`. **Don't add a new root `.md` file, and
don't add a new `docs/` top-level file either** unless it's genuinely a durable reference someone
opens on purpose (the bar the table above states) — that's the failure mode this reorg fixes:
`docs/` top-level had quietly become the new dumping ground the repo root used to be.

Before writing a new doc, ask what kind of document it is and route it accordingly:
- **A dated investigation, measurement, or "does this actually work" proof** → `docs/perf/`
  if it's about speed/bundle/render cost, `docs/incidents/` otherwise (a bug postmortem, a
  handoff brief, a critique/audit pass).
- **A script or checklist a human runs by hand** → `docs/testing/`.
- **A one-off artifact from a Cowork session (a brief, a result writeup)** → `docs/sessions/`.
- **A rule, schema, or catalog every session (or every session touching one subsystem) may need
  to open** → `docs/` top level, added to the table above in the same commit.

## Per-folder notes

Some module folders carry a short `CLAUDE.md` pointer (e.g. `src/workspaces/site-planner/`,
`src/workspaces/doc-review/`, `src/shared/`). Claude Code auto-loads a folder's `CLAUDE.md`
only when a session is working in that subtree — so they add targeted context without bloating
the root. **Keep them short pointers** (what's here + key files + where the deep detail is),
never duplicated detail, so they don't go stale.
